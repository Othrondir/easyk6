---
phase: 01-foundation-project-shape
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - .env.example
  - lib/config/runtime-config.ts
  - tests/unit/runtime-config.test.mjs
  - scripts/perf-runner.mjs
  - scripts/validate-build.mjs
  - k6/simulations/smoke/smoke-shell.test.ts
  - README.md
autonomous: true
requirements: [BUILD-01, BUILD-02]
user_setup: []
must_haves:
  truths:
    - `npm run smoke` resolves to explicit demo mode against the built-in QAbbalah URL unless CLI input overrides it.
    - Real-target runs fail fast with a clear error when `BASE_URL` is missing or invalid.
    - Runner shell can show effective config or dry-run the k6 command without hiding where execution will land.
  artifacts:
    - path: .env.example
      provides: Minimal root env contract
      contains: "BASE_URL="
    - path: lib/config/runtime-config.ts
      provides: Single source of truth for config precedence and validation
      contains: "CLI > .env > built-in demo defaults"
    - path: scripts/perf-runner.mjs
      provides: Public perf/smoke runner shell
      contains: "--show-config"
    - path: tests/unit/runtime-config.test.mjs
      provides: Automated precedence and fail-fast coverage
      contains: "BASE_URL is required when demo mode is disabled."
  key_links:
    - from: package.json
      to: scripts/perf-runner.mjs
      via: npm scripts `perf` and `smoke`
      pattern: "perf-runner.mjs"
    - from: scripts/perf-runner.mjs
      to: lib/config/runtime-config.ts
      via: shared config resolver import
      pattern: "runtime-config"
    - from: k6/simulations/smoke/smoke-shell.test.ts
      to: lib/config/runtime-config.ts
      via: shared base URL resolution
      pattern: "runtime-config"
---

<objective>
Add the first real config and runner contract on top of the new build foundation so later smoke and adaptation work plug into stable commands instead of inventing them.

Purpose: satisfy fail-fast configuration and make the `smoke` / `perf` surface concrete without yet implementing full scenario registry or upstream conversion.
Output: `.env.example`, shared runtime-config resolver, unit tests, `scripts/perf-runner.mjs`, and a smoke shell entrypoint upgraded from placeholder to a real minimal browser shell.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-project-shape/01-CONTEXT.md
@.planning/phases/01-foundation-project-shape/01-RESEARCH.md
@.planning/phases/01-foundation-project-shape/01-VALIDATION.md
@package.json
@vite.config.ts
@README.md
@legacy-js/config/config.js
@k6/simulations/smoke/smoke-shell.test.ts
@../easyPlaywright/playwright.config.ts
@../ir-perf-k6/scripts/k6-runner.js
</context>

<tasks>

<task type="auto">
  <name>Implement shared runtime config with root env convention and fail-fast validation</name>
  <files>.env.example, lib/config/runtime-config.ts, tests/unit/runtime-config.test.mjs, README.md</files>
  <read_first>package.json, README.md, legacy-js/config/config.js, .planning/phases/01-foundation-project-shape/01-CONTEXT.md, .planning/phases/01-foundation-project-shape/01-RESEARCH.md, ../easyPlaywright/playwright.config.ts</read_first>
  <action>Create root `.env.example` with only `BASE_URL=` and a short comment that `npm run smoke` uses the built-in QAbbalah URL in demo mode. Add `lib/config/runtime-config.ts` as the single source of truth for precedence and validation. Export a function that resolves config from CLI flags and environment using exact precedence `CLI > .env > built-in demo defaults`, where built-in demo default is `https://othrondir.github.io/QAbbalah/`. Support exact flags `--profile`, `--scenario`, `--base-url`, `--env-file`, `--demo`, `--show-config`, and `--dry-run`. When demo mode is false and no base URL is available, throw `Error('BASE_URL is required when demo mode is disabled.')`. When a URL is malformed, throw `Error('BASE_URL must be a valid absolute URL.')`. Return a normalized config object with `profile`, `scenario`, `baseUrl`, `demo`, `envFile`, `showConfig`, `dryRun`, and `entryFile`. Add `tests/unit/runtime-config.test.mjs` with Node 22 `node:test` coverage for: demo default path, `.env` loading, CLI override beating `.env`, invalid URL rejection, and missing `BASE_URL` when demo is disabled. Update README examples so config docs show root `.env`, `npm run smoke -- --dry-run`, and `npm run perf -- --profile smoke --base-url https://example.com`.</action>
  <verify>node --test tests/unit/runtime-config.test.mjs</verify>
  <acceptance_criteria>
    - `.env.example` contains `BASE_URL=`
    - lib/config/runtime-config.ts contains `CLI > .env > built-in demo defaults`
    - lib/config/runtime-config.ts contains `BASE_URL is required when demo mode is disabled.`
    - lib/config/runtime-config.ts contains `BASE_URL must be a valid absolute URL.`
    - tests/unit/runtime-config.test.mjs contains `CLI override beats env file`
    - README.md contains `npm run smoke -- --dry-run`
  </acceptance_criteria>
  <done>Config precedence is centralized, minimal, and automatically tested.</done>
</task>

<task type="auto">
  <name>Turn perf command into real runner shell and upgrade smoke shell entry</name>
  <files>package.json, scripts/perf-runner.mjs, scripts/validate-build.mjs, k6/simulations/smoke/smoke-shell.test.ts</files>
  <read_first>package.json, scripts/validate-build.mjs, k6/simulations/smoke/smoke-shell.test.ts, lib/config/runtime-config.ts, ../ir-perf-k6/scripts/k6-runner.js, vite.config.ts</read_first>
  <action>Add runtime dependencies `commander@11.1.0` and `dotenv@17.2.3` to `package.json`. Implement `scripts/perf-runner.mjs` as the public runner behind `npm run perf` and `npm run smoke`. Parse the exact flags from the shared config module, load `.env` or `--env-file` via `dotenv`, resolve config through `lib/config/runtime-config.ts`, and map Phase 1 smoke execution to `dist/tests/smoke/smoke-shell.test.js`. Support three non-destructive modes before full execution: `--show-config` prints normalized JSON, `--dry-run` prints the exact `k6 run dist/tests/smoke/smoke-shell.test.js` command plus resolved base URL, and normal execution spawns `k6` with inherited stdio. Upgrade `k6/simulations/smoke/smoke-shell.test.ts` from placeholder to a minimal browser shell that imports shared runtime config, sets a single Chromium smoke scenario, opens the resolved base URL, logs the selected profile/base URL, and closes cleanly. Strengthen `scripts/validate-build.mjs` so it verifies `dist/tests/smoke/smoke-shell.test.js`, `scripts/perf-runner.mjs`, `.env.example`, and `lib/config/runtime-config.ts` all exist, then prints a concise success summary.</action>
  <verify>npm run build && node scripts/validate-build.mjs && node scripts/perf-runner.mjs --profile smoke --demo --show-config && node scripts/perf-runner.mjs --profile smoke --dry-run --base-url https://example.com</verify>
  <acceptance_criteria>
    - package.json contains `commander` and `dotenv`
    - scripts/perf-runner.mjs contains `--show-config` and `--dry-run`
    - scripts/perf-runner.mjs contains `dist/tests/smoke/smoke-shell.test.js`
    - scripts/perf-runner.mjs contains `k6 run`
    - k6/simulations/smoke/smoke-shell.test.ts contains `browser: { type: 'chromium' }`
    - k6/simulations/smoke/smoke-shell.test.ts contains `await page.goto`
    - scripts/validate-build.mjs contains `lib/config/runtime-config.ts`
  </acceptance_criteria>
  <done>Public command grammar is real, smoke demo mode is explicit, and config errors are caught before k6 execution starts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/unit/runtime-config.test.mjs`
- [ ] `npm run build && node scripts/validate-build.mjs`
- [ ] `node scripts/perf-runner.mjs --profile smoke --demo --show-config`
- [ ] `node scripts/perf-runner.mjs --profile smoke --dry-run --base-url https://example.com`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Invalid real-target config fails before k6 execution
- `npm run smoke` has a documented demo-default path and `npm run perf` exposes the advanced CLI grammar
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-project-shape/01-02-SUMMARY.md`
</output>
