---
phase: 01-foundation-project-shape
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - .gitignore
  - README.md
  - PROJECT_STRUCTURE.md
  - scripts/perf-runner.mjs
  - scripts/sync-src.mjs
  - scripts/convert-pages.mjs
  - scripts/validate-build.mjs
  - k6/simulations/smoke/smoke-shell.test.ts
  - src/pages/.gitkeep
  - lib/pages/.gitkeep
  - lib/pages-k6-patches/.gitkeep
  - k6/scenarios/.gitkeep
  - legacy-js/**
autonomous: true
requirements: [BUILD-01]
user_setup: []
must_haves:
  truths:
    - Repository exposes a TypeScript/Vite build foundation instead of the old JS-only script surface.
    - Root folder layout makes `src/pages`, `lib/pages`, and `lib/pages-k6-patches` responsibilities obvious before Phase 2 begins.
    - Legacy starter assets remain available under `legacy-js/` without defining the new architecture.
  artifacts:
    - path: package.json
      provides: Phase 1 command surface with build/smoke/perf/sync/convert scripts
      contains: "\"build\""
    - path: vite.config.ts
      provides: Vite multi-entry CommonJS build for k6 simulations
      contains: "globSync('./k6/simulations/**/*.test.ts')"
    - path: k6/simulations/smoke/smoke-shell.test.ts
      provides: Stable build entry for the first smoke shell
      contains: "export const options"
    - path: scripts/perf-runner.mjs
      provides: Non-broken placeholder behind public perf/smoke commands
      contains: "Phase 1 foundation only."
    - path: README.md
      provides: New architecture-first narrative
      contains: "legacy-js"
  key_links:
    - from: package.json
      to: vite.config.ts
      via: npm build script
      pattern: "vite build"
    - from: package.json
      to: scripts/validate-build.mjs
      via: npm validate:build script
      pattern: "validate:build"
    - from: README.md
      to: PROJECT_STRUCTURE.md
      via: documented folder boundaries
      pattern: "src/pages|lib/pages|legacy-js"
---

<objective>
Establish build-friendly repo shape, TypeScript/Vite foundation, and architecture boundaries that later sync/conversion work can land on without another repo reshuffle.

Purpose: satisfy the "modern build path" and "clear folder responsibilities" parts of Phase 1 before real config and runner behavior arrive.
Output: a working Vite build, placeholder public commands, first-class TypeScript/k6 directories, and legacy starter assets moved under `legacy-js/`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-project-shape/01-CONTEXT.md
@.planning/phases/01-foundation-project-shape/01-RESEARCH.md
@package.json
@README.md
@PROJECT_STRUCTURE.md
@config/config.js
@tests/smoke/basic-smoke.test.js
@pages/BasePage.js
@../easyPlaywright/package.json
@../ir-perf-k6/package.json
@../ir-perf-k6/vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Install TypeScript/Vite foundation and stable placeholder build entry</name>
  <files>package.json, tsconfig.json, vite.config.ts, .gitignore, scripts/perf-runner.mjs, scripts/sync-src.mjs, scripts/convert-pages.mjs, scripts/validate-build.mjs, k6/simulations/smoke/smoke-shell.test.ts</files>
  <read_first>package.json, README.md, .planning/phases/01-foundation-project-shape/01-CONTEXT.md, .planning/phases/01-foundation-project-shape/01-RESEARCH.md, ../ir-perf-k6/package.json, ../ir-perf-k6/vite.config.ts</read_first>
  <action>Rewrite `package.json` to the new command surface and toolchain. Set `"type": "module"`. Keep project name/version/license, then define exact scripts: `"build": "vite build"`, `"build:watch": "vite build --watch"`, `"validate:build": "node scripts/validate-build.mjs"`, `"perf": "node scripts/perf-runner.mjs"`, `"smoke": "node scripts/perf-runner.mjs --profile smoke --demo"`, `"sync:src": "node scripts/sync-src.mjs"`, `"convert-pages": "node scripts/convert-pages.mjs"`. Add exact devDependencies `typescript@5.9.3`, `vite@5.4.21`, `glob@10.4.5`, `vite-plugin-node-polyfills@0.19.0`, and `@types/k6@0.45.3`. Create `tsconfig.json` with `baseUrl: "."`, strict TypeScript settings, and exact path aliases: `@lib/* -> lib/*`, `@config -> lib/config/runtime-config.ts`, `@pages/* -> lib/pages/*`, `@src-pages/* -> src/pages/*`, `@k6/* -> k6/*`. Create `vite.config.ts` that scans `./k6/simulations/**/*.test.ts` with `globSync`, maps entries to `dist/tests/...`, outputs `formats: ['cjs']`, disables minify, externalizes `k6`, `k6/*`, `k6/browser`, and `k6/metrics`, and wires aliases for `@lib`, `@config`, `@pages`, `@src-pages`, and `@k6`. Create `scripts/perf-runner.mjs` as a temporary placeholder that prints `Phase 1 foundation only. Re-run after Plan 01-02 for real execution.` and exits 0 so `npm run smoke` and `npm run perf` are not broken between waves. Create `scripts/sync-src.mjs` and `scripts/convert-pages.mjs` as truthful Phase 2 placeholders that print `Phase 2 owns this implementation.` and exit 0. Create `scripts/validate-build.mjs` that fails when `dist/tests/smoke/smoke-shell.test.js` is missing. Create `k6/simulations/smoke/smoke-shell.test.ts` as a minimal placeholder with `export const options = { vus: 1, iterations: 1 }` and a no-op default function so `npm run build` succeeds before the real runner shell exists. Update `.gitignore` to ignore `dist/`, `.env`, and `reports/`.</action>
  <verify>npm install && npm run build && node scripts/validate-build.mjs</verify>
  <acceptance_criteria>
    - package.json contains `"build": "vite build"` and `"smoke": "node scripts/perf-runner.mjs --profile smoke --demo"`
    - tsconfig.json contains `"@pages/*": ["lib/pages/*"]` and `"@src-pages/*": ["src/pages/*"]`
    - vite.config.ts contains `globSync('./k6/simulations/**/*.test.ts')` and `formats: ['cjs']`
    - scripts/perf-runner.mjs contains `Phase 1 foundation only.`
    - scripts/sync-src.mjs contains `Phase 2 owns this implementation.`
    - scripts/validate-build.mjs contains `dist/tests/smoke/smoke-shell.test.js`
    - k6/simulations/smoke/smoke-shell.test.ts contains `export const options`
    - .gitignore contains `dist/` and `.env`
  </acceptance_criteria>
  <done>Build command is real, build output target is stable, and placeholder Phase 2 commands exist without pretending to be implemented.</done>
</task>

<task type="auto">
  <name>Move starter assets under legacy-js and document new repo boundaries</name>
  <files>legacy-js/**, src/pages/.gitkeep, lib/pages/.gitkeep, lib/pages-k6-patches/.gitkeep, k6/scenarios/.gitkeep, README.md, PROJECT_STRUCTURE.md</files>
  <read_first>README.md, PROJECT_STRUCTURE.md, config/config.js, pages/BasePage.js, tests/smoke/basic-smoke.test.js, .planning/phases/01-foundation-project-shape/01-CONTEXT.md</read_first>
  <action>Move the current starter JavaScript assets into `legacy-js/` while preserving internal relative paths: `pages/ -> legacy-js/pages/`, `tests/ -> legacy-js/tests/`, `utils/ -> legacy-js/utils/`, `examples/ -> legacy-js/examples/`, and the current JavaScript config files under `legacy-js/config/`. Leave the new root architecture clean by creating `src/pages/`, `lib/pages/`, `lib/pages-k6-patches/`, and `k6/scenarios/` as first-class directories using `.gitkeep` files where needed. Rewrite `README.md` and `PROJECT_STRUCTURE.md` so the first architecture tree shown to a reviewer is the new TypeScript layout, with exact boundary labels: `src/pages = synced upstream Playwright source`, `lib/pages = generated k6-compatible output`, `lib/pages-k6-patches = persistent k6-only overrides`, and `legacy-js = archived starter reference`. Keep the legacy note short; do not let the old starter tree remain the opening explanation.</action>
  <verify>rg -n "legacy-js|src/pages|lib/pages|lib/pages-k6-patches|npm run build" README.md PROJECT_STRUCTURE.md</verify>
  <acceptance_criteria>
    - `legacy-js/pages/BasePage.js` exists
    - `legacy-js/tests/smoke/basic-smoke.test.js` exists
    - `legacy-js/config/config.js` exists
    - `src/pages/.gitkeep` exists
    - `lib/pages/.gitkeep` exists
    - `lib/pages-k6-patches/.gitkeep` exists
    - README.md contains `src/pages = synced upstream Playwright source`
    - README.md contains `legacy-js`
    - PROJECT_STRUCTURE.md contains `lib/pages-k6-patches`
  </acceptance_criteria>
  <done>The repo reads as TypeScript-first at a glance, and old JS assets survive only as historical reference.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm install && npm run build && node scripts/validate-build.mjs`
- [ ] `node scripts/perf-runner.mjs --profile smoke --demo` exits 0 and prints placeholder text
- [ ] `node scripts/sync-src.mjs` exits 0 and prints placeholder text
- [ ] `node scripts/convert-pages.mjs` exits 0 and prints placeholder text
- [ ] README and PROJECT_STRUCTURE both show the new architecture before mentioning legacy-js
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Build command produces `dist/tests/smoke/smoke-shell.test.js`
- Root repo structure communicates upstream vs generated vs custom boundaries clearly
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-project-shape/01-01-SUMMARY.md`
</output>
