---
phase: 01-foundation-project-shape
plan: "03"
type: execute
gap_closure: true
wave: 3
depends_on: ["01-02"]
files_modified:
  - scripts/perf-runner.mjs
  - tests/unit/perf-runner.test.mjs
  - README.md
autonomous: true
requirements: [BUILD-02]
user_setup: []
must_haves:
  truths:
    - The public runner accepts `BASE_URL` supplied directly from the shell when `.env` is absent.
    - `.env` remains the main developer-facing config source and still overrides shell-provided `BASE_URL` when both are present.
    - README documents the shell-env runtime path alongside the existing `.env` and CLI examples.
  artifacts:
    - path: scripts/perf-runner.mjs
      provides: Public runner shell that merges shell env with parsed `.env` data before resolving config
      contains: "mergeRuntimeEnv"
    - path: tests/unit/perf-runner.test.mjs
      provides: Runner-level automated coverage for shell env and `.env` precedence
      contains: "shell-provided BASE_URL"
    - path: README.md
      provides: User-facing documentation for shell env configuration
      contains: "Set `BASE_URL` in your shell"
  key_links:
    - from: scripts/perf-runner.mjs
      to: lib/config/runtime-config.ts
      via: merged env passed into `resolveRuntimeConfig()`
      pattern: "resolveRuntimeConfig"
    - from: tests/unit/perf-runner.test.mjs
      to: scripts/perf-runner.mjs
      via: child process dry-run execution
      pattern: "perf-runner.mjs"
    - from: README.md
      to: scripts/perf-runner.mjs
      via: documented shell env dry-run command
      pattern: "npm run perf -- --profile smoke --dry-run"
---

<objective>
Close the remaining Phase 1 BUILD-02 gap by teaching the public runner to honor shell-provided `BASE_URL` without weakening the existing `.env` and CLI contract.

Purpose: Phase 1 already has the build path, runtime-config module, and perf/smoke shell. The only blocker left is that `scripts/perf-runner.mjs` ignores `process.env.BASE_URL`, so env-var configuration is not actually supported through the public entrypoint.
Output: merged env resolution in `scripts/perf-runner.mjs`, runner-level automated coverage in `tests/unit/perf-runner.test.mjs`, and README guidance for shell-env usage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-project-shape/01-CONTEXT.md
@.planning/phases/01-foundation-project-shape/01-RESEARCH.md
@.planning/phases/01-foundation-project-shape/01-VALIDATION.md
@.planning/phases/01-foundation-project-shape/01-02-SUMMARY.md
@.planning/phases/01-foundation-project-shape/01-VERIFICATION.md
@README.md
@scripts/perf-runner.mjs
@lib/config/runtime-config.ts
@tests/unit/runtime-config.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Merge shell env into runner config resolution without breaking the existing precedence contract</name>
  <files>scripts/perf-runner.mjs</files>
  <read_first>scripts/perf-runner.mjs, lib/config/runtime-config.ts, .planning/phases/01-foundation-project-shape/01-VERIFICATION.md, README.md</read_first>
  <action>In `scripts/perf-runner.mjs`, add an explicit helper named `mergeRuntimeEnv(shellEnv, loadedEnv)` that returns a merged object where parsed `.env` values override shell values: `{ ...shellEnv, ...loadedEnv }`. In `main()`, keep `loadRuntimeEnv()` unchanged, then create `const mergedEnv = mergeRuntimeEnv(process.env, loadedEnv)` and pass that exact `mergedEnv` object into `resolveRuntimeConfig({ ...cliOptions, envFile }, mergedEnv)`. Keep CLI precedence unchanged by leaving `resolveRuntimeConfig()` in `lib/config/runtime-config.ts` as the single resolver. Also pass `mergedEnv` into `toRunnerEnv()` so the spawned k6 process inherits the merged environment plus the normalized `BASE_URL`, `K6_PROFILE`, `K6_SCENARIO`, and `K6_DEMO` values. Do not add silent fallbacks or change the existing error strings for missing or malformed `BASE_URL`.</action>
  <verify>$env:BASE_URL='https://shell.example.test'; try { node scripts/perf-runner.mjs --profile smoke --dry-run } finally { Remove-Item Env:BASE_URL -ErrorAction SilentlyContinue }</verify>
  <acceptance_criteria>
    - scripts/perf-runner.mjs contains `function mergeRuntimeEnv`
    - scripts/perf-runner.mjs contains `const mergedEnv = mergeRuntimeEnv(process.env, loadedEnv)`
    - scripts/perf-runner.mjs contains `resolveRuntimeConfig(` and `mergedEnv`
    - scripts/perf-runner.mjs contains `K6_PROFILE` and `K6_SCENARIO`
    - `node scripts/perf-runner.mjs --profile smoke --dry-run` prints `Resolved base URL: https://shell.example.test/` when `BASE_URL` is supplied through the shell
  </acceptance_criteria>
  <done>The public runner resolves shell-provided `BASE_URL` correctly while keeping `.env` as the higher-priority config source.</done>
</task>

<task type="auto">
  <name>Add runner-level env-path coverage and document the shell-env workflow</name>
  <files>tests/unit/perf-runner.test.mjs, README.md</files>
  <read_first>tests/unit/runtime-config.test.mjs, scripts/perf-runner.mjs, README.md, .planning/phases/01-foundation-project-shape/01-VERIFICATION.md</read_first>
  <action>Create `tests/unit/perf-runner.test.mjs` with Node 22 `node:test` coverage that executes `node scripts/perf-runner.mjs --profile smoke --dry-run` as a child process from the repo root. Add one test named `shell-provided BASE_URL is accepted by the public runner` that injects `BASE_URL=https://shell.example.test` through the child process environment and asserts stdout contains `Resolved base URL: https://shell.example.test/` and `k6 run dist/tests/smoke/smoke-shell.test.js`. Add one test named `env file beats shell BASE_URL in the public runner` that creates a temporary env file containing `BASE_URL=https://env.example.test`, also sets shell `BASE_URL=https://shell.example.test`, runs `node scripts/perf-runner.mjs --profile smoke --env-file <tempfile> --dry-run`, and asserts stdout contains `Resolved base URL: https://env.example.test/`. Update the README Runtime Config section to state the exact effective precedence `CLI > .env > shell env > built-in demo defaults`, keep the existing `.env` and CLI examples, and add one short sentence that says `Set \`BASE_URL\` in your shell and run \`npm run perf -- --profile smoke --dry-run\`` when you want to avoid a `.env` file.</action>
  <verify>node --test tests/unit/runtime-config.test.mjs tests/unit/perf-runner.test.mjs</verify>
  <acceptance_criteria>
    - tests/unit/perf-runner.test.mjs contains `shell-provided BASE_URL is accepted by the public runner`
    - tests/unit/perf-runner.test.mjs contains `env file beats shell BASE_URL in the public runner`
    - tests/unit/perf-runner.test.mjs contains `k6 run dist/tests/smoke/smoke-shell.test.js`
    - README.md contains `CLI > .env > shell env > built-in demo defaults`
    - README.md contains `Set \`BASE_URL\` in your shell`
    - README.md contains `npm run perf -- --profile smoke --dry-run`
  </acceptance_criteria>
  <done>The env-var path is covered by automation and documented as a supported part of the public runner contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/unit/runtime-config.test.mjs tests/unit/perf-runner.test.mjs`
- [ ] `npm run build && node scripts/validate-build.mjs`
- [ ] `$env:BASE_URL='https://shell.example.test'; try { node scripts/perf-runner.mjs --profile smoke --dry-run } finally { Remove-Item Env:BASE_URL -ErrorAction SilentlyContinue }`
- [ ] `node scripts/perf-runner.mjs --profile smoke --env-file .env.example --dry-run` fails fast with `BASE_URL is required when demo mode is disabled.`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Public runner accepts shell-provided `BASE_URL` when `.env` is absent
- `.env` remains the documented higher-priority source over shell env values
- Phase 1 verification can pass BUILD-02 after re-running the verifier
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-project-shape/01-03-SUMMARY.md`
</output>
