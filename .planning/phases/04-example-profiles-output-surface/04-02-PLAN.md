---
phase: 04-example-profiles-output-surface
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - lib/simulations/load.ts
  - lib/simulations/capacity.ts
  - lib/config/runtime-config.ts
  - package.json
  - scripts/validate-build.mjs
  - tests/unit/simulations-load.test.mjs
  - tests/unit/simulations-capacity.test.mjs
  - tests/unit/perf-runner.test.mjs
  - README.md
autonomous: false
tags:
  - phase-4
  - prof-02
  - prof-03
  - prof-04
  - load-profile
  - capacity-profile
  - example-labeling
  - validate-build
  - readme-quickstart-table
requirements:
  - PROF-02
  - PROF-03
  - PROF-04
must_haves:
  truths:
    - "`lib/simulations/load.ts` exists, starts with the EXAMPLE PROFILE JSDoc banner per D-14 (multi-line `/** EXAMPLE PROFILE — load testing shape. ... */`), imports the same surface as smoke.ts (`browser` from `k6/browser`, `exec` from `k6/execution`, `resolveRuntimeConfig` from `@config`, `K6PlaywrightSelectors` from `@pages/base/selectors`, `SCENARIO_REGISTRY` from `@lib/scenarios`, `makeHandleSummary` from `./lib/summary`), and exports an `options` literal with executor `ramping-vus`, startVUs 0, stages `[{duration:'30s',target:5},{duration:'60s',target:5},{duration:'30s',target:0}]`, gracefulRampDown `'30s'`, options.browser.type `'chromium'`, and thresholds VERBATIM per the 2026-05-11 amended D-03: `browser_web_vital_lcp:['p(95)<4000']`, `http_req_failed:['rate<0.05']`, `iteration_duration:['p(95)<25000']`, `browser_http_req_duration:['p(95)<2000']` (NOT `http_req_duration` — the CONTEXT amendment retargeted to the browser metric per RESEARCH §5 Pitfall 1)."
    - "`lib/simulations/load.ts` default function follows the smoke.ts dispatch pattern: resolveRuntimeConfig → read __ENV.SCENARIO with `?? 'home-smoke'` fallback per D-02 → SCENARIO_REGISTRY lookup → `exec.test.abort` fail-fast on unknown → `browser.newPage()` + `new K6PlaywrightSelectors(page)` → console.log announce banner → `await page.goto(runtimeConfig.baseUrl)` Q3-LANDMINE FIX → `await entry.fn({ page, selectors })` → `page.close()` in finally."
    - "`lib/simulations/load.ts` exports `handleSummary` via `makeHandleSummary({ profile: 'load', scenarioGetter: () => __ENV.SCENARIO ?? 'home-smoke', baseUrlGetter: () => __ENV.BASE_URL ?? '' });` immediately after the `options` literal."
    - "`lib/simulations/capacity.ts` exists with the same banner + import surface as load.ts (D-14), exports an `options` literal with executor `ramping-arrival-rate`, startRate 0, timeUnit `'1s'`, preAllocatedVUs 10, maxVUs 10, stages `[{duration:'180s',target:10}]`, options.browser.type `'chromium'`, and thresholds VERBATIM per the 2026-05-11 amended D-08: `browser_web_vital_lcp:['p(95)<4000']`, `http_req_failed:['rate<0.05']`, `browser_http_req_duration:['p(95)<3000']` (NOT `http_req_duration`), `iteration_duration:['p(95)<30000']`."
    - "`lib/simulations/capacity.ts` default function follows the same dispatch pattern as load.ts (including the Q3 landmine page.goto fix) and exports `handleSummary` via `makeHandleSummary({ profile: 'capacity', ... })`."
    - "`tests/unit/simulations-load.test.mjs` exists and asserts the load options literal verbatim (executor + 3 stages + chromium + 4 amended thresholds) via the multi-source data-URL loader pattern from `tests/unit/scenarios-registry.test.mjs::loadSmokeOptions`. All 5+ assertions green."
    - "`tests/unit/simulations-capacity.test.mjs` exists and asserts the capacity options literal verbatim (executor + ramping-arrival-rate params + 4 amended thresholds). All 5+ assertions green."
    - "`tests/unit/perf-runner.test.mjs` is extended with two new tests: (a) `--profile load --dry-run` prints `k6 run ... dist/simulations/load.js`, (b) `--profile capacity --dry-run` prints `k6 run ... dist/simulations/capacity.js`. Existing perf-runner tests stay green."
    - "`lib/config/runtime-config.ts` no longer exports `PHASE_ONE_SMOKE_ENTRY_FILE`. Grep `PHASE_ONE_SMOKE_ENTRY_FILE` in `lib/`, `scripts/`, and `tests/` returns zero matches. The const + its JSDoc preamble are deleted; the `SMOKE_ENTRY_FILE` const is retained (still used by `tests/unit/runtime-config.test.mjs`)."
    - "`package.json` `scripts` block has two new entries — `example:load` (= `node scripts/perf-runner.mjs --profile load --demo`) and `example:capacity` (= `node scripts/perf-runner.mjs --profile capacity --demo`). Order matches recruiter-readable convention: `build`, `build:watch`, `validate:build`, `perf`, `smoke`, `example:load`, `example:capacity`, `sync:src`, `convert-pages` — smoke remains the supported demo path at the top of the runtime section; example:* immediately follows."
    - "`scripts/validate-build.mjs::requiredFiles` is extended (strict per CONTEXT recommendation + D-17(f)) to include `'dist/simulations/load.js'` and `'dist/simulations/capacity.js'` as new entries alongside the existing 4. Final list (6 entries): `['dist/simulations/smoke.js', 'dist/simulations/load.js', 'dist/simulations/capacity.js', 'scripts/perf-runner.mjs', '.env.example', 'lib/config/runtime-config.ts']`."
    - "`npm run build && npm run validate:build` exits 0 — Vite glob (vite.config.ts:32) auto-picks both new entries and emits `dist/simulations/load.js` + `dist/simulations/capacity.js` without any vite.config.ts edit."
    - "`README.md` Quickstart section contains a 3-row Supported-vs-Example table per D-13/D-15. Exact 3 rows VERBATIM: `| \\`npm run smoke\\` | **Supported** | Default smoke profile against the demo target (recruiter demo path). |`, `| \\`npm run example:load\\` | _Example_ | Illustrative load profile (ramping-vus, 5 VUs, ~2 min). |`, `| \\`npm run example:capacity\\` | _Example_ | Illustrative capacity profile (ramping-arrival-rate, find-the-ceiling, ~3 min). |`. Header row: `| Command | Status | What it does |`. Located in the existing `## Commands` section OR a new `## Quickstart` subsection above `## Commands` (Phase 5 polishes prose; Phase 4 lands only the table contract per CONTEXT D-15)."
    - "Verify wave (checkpoint): real `npm run example:load -- --demo` against https://othrondir.github.io/QAbbalah/ completes the 2-min ramp, exits 0 (or 99 if a soft threshold trips — both are recruiter-evidence-valid per D-04), and writes `reports/load-home-smoke.md` + `reports/load-home-smoke.json` to the filesystem. Markdown contains all 4 D-03 threshold rows; the `browser_http_req_duration` row shows a real ms value (not `n/a`)."
    - "Verify wave (checkpoint): real `npm run example:capacity -- --demo` completes the 3-min ramp, exits 0 or 99, writes `reports/capacity-home-smoke.md` + `reports/capacity-home-smoke.json`. Markdown contains all 4 D-08 threshold rows; at least one threshold trip is the recruiter narrative (D-05 'find-the-breaking-point')."
    - "Verify wave (checkpoint): real `npm run smoke` STILL exits 0 (regression — Phase 3 D-66 contract intact), all 3 D-66 thresholds pass, AND it now writes `reports/smoke-home-smoke.md` + `reports/smoke-home-smoke.json` (PROF-04 end-to-end evidence for the supported profile)."
    - "Final green-bar gate: `npm test && npm run build && npm run validate:build` exits 0. Test count: Phase 3 baseline 84 + Plan 04-01 new 12 + Plan 04-02 new ~12 = ~108 tests."
  artifacts:
    - path: "lib/simulations/load.ts"
      provides: "Example load profile. ramping-vus executor + 3-stage shape + chromium browser + 4 amended thresholds + makeHandleSummary({profile:'load'}). EXAMPLE PROFILE banner per D-14."
      contains: "ramping-vus"
      min_lines: 90
    - path: "lib/simulations/capacity.ts"
      provides: "Example capacity profile. ramping-arrival-rate executor + 180s ramp to 10 iter/s + chromium browser + 4 amended thresholds + makeHandleSummary({profile:'capacity'}). EXAMPLE PROFILE banner per D-14."
      contains: "ramping-arrival-rate"
      min_lines: 90
    - path: "lib/config/runtime-config.ts"
      provides: "Same Phase 3 file, with PHASE_ONE_SMOKE_ENTRY_FILE constant + its preamble removed per D-18. SMOKE_ENTRY_FILE retained."
      contains: "SMOKE_ENTRY_FILE"
    - path: "scripts/validate-build.mjs"
      provides: "requiredFiles list extended from 4 to 6 entries — adds dist/simulations/load.js and dist/simulations/capacity.js."
      contains: "dist/simulations/load.js"
    - path: "package.json"
      provides: "scripts block extended with example:load and example:capacity. Order: build / build:watch / validate:build / perf / smoke / example:load / example:capacity / sync:src / convert-pages."
      contains: "example:load"
    - path: "tests/unit/simulations-load.test.mjs"
      provides: "Asserts lib/simulations/load.ts options literal verbatim per D-01 + amended D-03 thresholds."
      min_lines: 90
    - path: "tests/unit/simulations-capacity.test.mjs"
      provides: "Asserts lib/simulations/capacity.ts options literal verbatim per D-06/D-07 + amended D-08 thresholds."
      min_lines: 90
    - path: "tests/unit/perf-runner.test.mjs"
      provides: "Extended with --profile load and --profile capacity dry-run cases (existing tests untouched)."
      contains: "dist/simulations/load.js"
    - path: "README.md"
      provides: "Adds the 3-row Supported-vs-Example table in the Quickstart section per D-13/D-15."
      contains: "**Supported**"
  key_links:
    - from: "lib/simulations/load.ts"
      to: "lib/simulations/lib/summary.ts (factory) + lib/scenarios/index.ts (registry)"
      via: "ESM imports — `from './lib/summary'` and `from '@lib/scenarios'`"
      pattern: "makeHandleSummary\\(\\{ profile: 'load'"
    - from: "lib/simulations/capacity.ts"
      to: "lib/simulations/lib/summary.ts (factory) + lib/scenarios/index.ts (registry)"
      via: "ESM imports — same shape as load.ts"
      pattern: "makeHandleSummary\\(\\{ profile: 'capacity'"
    - from: "scripts/perf-runner.mjs::buildK6Args (unchanged)"
      to: "dist/simulations/load.js + dist/simulations/capacity.js"
      via: "runtime-config.ts::resolveEntryFile is already profile-keyed (`dist/simulations/${profile}.js`) — no resolver change needed for load/capacity"
      pattern: "dist/simulations/\\$\\{profile\\}\\.js"
    - from: "package.json::scripts.example:load + example:capacity"
      to: "scripts/perf-runner.mjs --profile load|capacity --demo"
      via: "Direct npm-script command lines"
      pattern: "\"example:(load|capacity)\":"
    - from: "scripts/validate-build.mjs::requiredFiles"
      to: "dist/simulations/load.js + dist/simulations/capacity.js"
      via: "Extended static list (strict validation)"
      pattern: "dist/simulations/(load|capacity)\\.js"
---

<objective>
Plan 04-02 lands the **example load + capacity simulation entries**, the **package.json `example:*` scripts**, the **strict validate-build extension**, the **`PHASE_ONE_SMOKE_ENTRY_FILE` cleanup (D-18)**, and the **README quickstart Supported-vs-Example table contract (D-15)** — and then captures **real-run recruiter evidence** for all three profiles (smoke regression + load + capacity) against the QAbbalah demo target.

It depends on Plan 04-01 (`makeHandleSummary` factory + smoke regression) being green; both new entries plug into the same factory with one-line `export const handleSummary = makeHandleSummary({ profile: 'X' })` invocations.

Purpose: PROF-02 ("Load profile code exists as an example within the same runner/config system") + PROF-03 ("Capacity profile code exists as an example within the same runner/config system") + PROF-04 ("Smoke execution produces a readable summary or report-ready artifact"). The end state is a recruiter clone of the repo running `npm run smoke` / `npm run example:load` / `npm run example:capacity` and seeing 6 artifacts in `reports/` plus a 3-row Quickstart table that explains which is which.

Output: Two new simulation files; two new test files; two new npm scripts; one extended validate-build list; one runtime-config cleanup (D-18); one README table; 6 real-run report artifacts captured in 04-02-SUMMARY.md; full green-bar gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-example-profiles-output-surface/04-CONTEXT.md
@.planning/phases/04-example-profiles-output-surface/04-RESEARCH.md
@.planning/phases/04-example-profiles-output-surface/04-VALIDATION.md
@.planning/phases/04-example-profiles-output-surface/04-01-PLAN.md
@.planning/phases/03-smoke-scenarios-supported-execution/03-02-SUMMARY.md
@lib/simulations/smoke.ts
@lib/config/runtime-config.ts
@scripts/validate-build.mjs
@scripts/perf-runner.mjs
@package.json
@vite.config.ts
@tests/unit/scenarios-registry.test.mjs
@tests/unit/perf-runner.test.mjs
@README.md

<interfaces>
<!-- The exact contracts the executor needs. Extracted from codebase. -->

From `lib/simulations/smoke.ts` (Phase 3 — DIRECT analog for load.ts + capacity.ts dispatch shape):
```typescript
import { browser } from 'k6/browser';
import exec from 'k6/execution';

import { resolveRuntimeConfig } from '@config';
import { K6PlaywrightSelectors } from '@pages/base/selectors';
import { SCENARIO_REGISTRY } from '@lib/scenarios';
import { makeHandleSummary } from './lib/summary';  // Plan 04-01

declare const __ENV: Record<string, string | undefined>;

export const options = { /* DIFFERENT per profile */ };

export const handleSummary = makeHandleSummary({
  profile: 'smoke',  // 'load' for load.ts, 'capacity' for capacity.ts
  scenarioGetter: () => __ENV.SCENARIO ?? 'home-smoke',
  baseUrlGetter: () => __ENV.BASE_URL ?? '',
});

export default async function smokeSimulation(): Promise<void> {
  const runtimeConfig = resolveRuntimeConfig(
    {
      profile: __ENV.K6_PROFILE,
      scenario: __ENV.K6_SCENARIO,
      baseUrl: __ENV.BASE_URL,
      demo: __ENV.K6_DEMO === 'true',
    },
    __ENV
  );

  const scenarioId = __ENV.SCENARIO ?? 'home-smoke';
  const entry = SCENARIO_REGISTRY[scenarioId];

  if (!entry) {
    const available = Object.keys(SCENARIO_REGISTRY).join(', ');
    const message = `Unknown scenario '${scenarioId}'. Available: ${available}`;
    // exec.test.abort triggers non-zero exit (SCEN-03 fail-fast — Phase 03-02 deviation D-04).
    exec.test.abort(message);
    throw new Error(message);
  }

  const page = await browser.newPage();
  const selectors = new K6PlaywrightSelectors(page);

  console.log(
    `[easyk6] <PROFILE> scenario=${scenarioId} profile=${runtimeConfig.profile} baseUrl=${runtimeConfig.baseUrl}`
  );
  console.log(`[easyk6] ${entry.description}`);

  try {
    // Q3 LANDMINE FIX (Phase 03-01 D-A7): HomePage.pageUrl='' so K6Page.navigate()
    // would no-op. The entry MUST land the browser on the demo target before
    // handing off to the scenario.
    await page.goto(runtimeConfig.baseUrl);
    await entry.fn({ page, selectors });
  } finally {
    await page.close();
  }
}
```

From `lib/config/runtime-config.ts` (CURRENT shape — D-18 deletes lines 5-12):
```typescript
// Lines 1-12 (BEFORE D-18 cleanup):
export const DEMO_BASE_URL = 'https://othrondir.github.io/QAbbalah/';
export const DEFAULT_ENV_FILE = '.env';
export const DEFAULT_PROFILE = 'smoke';
export const DEFAULT_SCENARIO = 'home-smoke';
/**
 * SMOKE_ENTRY_FILE is the canonical Phase 3 entry path (D-62).
 * PHASE_ONE_SMOKE_ENTRY_FILE remains exported only as a transition-period
 * alias consumed by validate-build.mjs until Plan 03-02 deletes the Phase 1
 * shell. Do NOT introduce new consumers of PHASE_ONE_SMOKE_ENTRY_FILE.
 */
export const SMOKE_ENTRY_FILE = 'dist/simulations/smoke.js';
export const PHASE_ONE_SMOKE_ENTRY_FILE = 'dist/tests/smoke/smoke-shell.test.js';

// Lines 1-6 (AFTER D-18 cleanup — JSDoc preamble dropped, alias deleted):
export const DEMO_BASE_URL = 'https://othrondir.github.io/QAbbalah/';
export const DEFAULT_ENV_FILE = '.env';
export const DEFAULT_PROFILE = 'smoke';
export const DEFAULT_SCENARIO = 'home-smoke';
export const SMOKE_ENTRY_FILE = 'dist/simulations/smoke.js';
```

From `scripts/validate-build.mjs` (CURRENT — 4 required files; this plan extends to 6):
```javascript
const requiredFiles = [
  'dist/simulations/smoke.js',
  'scripts/perf-runner.mjs',
  '.env.example',
  'lib/config/runtime-config.ts',
];
// After this plan (strict per D-17(f) + CONTEXT recommendation):
const requiredFiles = [
  'dist/simulations/smoke.js',
  'dist/simulations/load.js',       // NEW
  'dist/simulations/capacity.js',   // NEW
  'scripts/perf-runner.mjs',
  '.env.example',
  'lib/config/runtime-config.ts',
];
```

From `vite.config.ts:32` (UNCHANGED — already auto-picks new entries):
```typescript
const simulationFiles = globSync('./lib/simulations/**/*.ts');
// Picks up smoke.ts + load.ts + capacity.ts automatically. No edit needed.
```

From `scripts/perf-runner.mjs::buildK6Args` (UNCHANGED — already passes 5 -e flags including SCENARIO):
```javascript
return [
  'run',
  '-e', `SCENARIO=${runtimeConfig.scenario}`,
  '-e', `BASE_URL=${runtimeConfig.baseUrl}`,
  '-e', `K6_PROFILE=${runtimeConfig.profile}`,
  '-e', `K6_SCENARIO=${runtimeConfig.scenario}`,
  '-e', `K6_DEMO=${String(runtimeConfig.demo)}`,
  runtimeConfig.entryFile,
];
```

From `lib/config/runtime-config.ts::resolveEntryFile` (UNCHANGED — already profile-keyed):
```typescript
function resolveEntryFile(profile: string): string {
  return `dist/simulations/${profile}.js`;
}
// --profile load  → dist/simulations/load.js
// --profile capacity → dist/simulations/capacity.js
// Zero runtime-config changes needed for new profiles.
```

From `tests/unit/scenarios-registry.test.mjs::loadSmokeOptions` (lines 124-180 — REUSE the multi-source data-URL pattern for the two new simulations tests). After Plan 04-01 the stubs object includes `'./lib/summary'`. For load + capacity tests, the SAME six stubs apply:
```javascript
const stubs = {
  'k6/browser': 'export const browser = { newPage: async () => null };',
  'k6/execution': 'export default { test: { abort: () => {} } };',
  '@lib/scenarios': 'export const SCENARIO_REGISTRY = {};',
  '@pages/base/selectors': 'export class K6PlaywrightSelectors { constructor() {} }',
  '@config': 'export function resolveRuntimeConfig() { return { /* ... */ }; }',
  './lib/summary': 'export function makeHandleSummary() { return () => ({}); }',
};
```

**EXAMPLE PROFILE banner (D-14 verbatim — copy-paste into both load.ts and capacity.ts):**
```typescript
/**
 * EXAMPLE PROFILE — load testing shape.
 * Smoke is the supported demo path; this profile is illustrative of how
 * the same architecture (registry, runner, summary) scales beyond smoke.
 * See README §Profiles.
 */
```
(For capacity.ts, change "load testing shape" → "capacity testing shape".)

**Threshold strings — VERBATIM amended D-03 (for load.ts) per 2026-05-11 CONTEXT amendment + RESEARCH §5 Pitfall 1:**
```typescript
thresholds: {
  browser_web_vital_lcp: ['p(95)<4000'],
  http_req_failed: ['rate<0.05'],
  iteration_duration: ['p(95)<25000'],
  browser_http_req_duration: ['p(95)<2000'],  // RETARGETED from http_req_duration
}
```

**Threshold strings — VERBATIM amended D-08 (for capacity.ts) per 2026-05-11 CONTEXT amendment:**
```typescript
thresholds: {
  browser_web_vital_lcp: ['p(95)<4000'],
  http_req_failed: ['rate<0.05'],
  browser_http_req_duration: ['p(95)<3000'],  // RETARGETED from http_req_duration
  iteration_duration: ['p(95)<30000'],
}
```

**CRITICAL: DO NOT use the string `http_req_duration` in either threshold map. The CONTEXT amendment explicitly retargeted both D-03 and D-08 to `browser_http_req_duration` after Phase 03-02 SUMMARY Run 1 empirically proved `http_req_*` metrics have 0 samples in browser scenarios (`http_req_failed.............: 0.00%  0 out of 0` from real run).**

**3-row README quickstart table (D-13 + D-15 verbatim — Phase 4 lands the contract, Phase 5 polishes prose):**
```markdown
| Command | Status | What it does |
|---|---|---|
| `npm run smoke` | **Supported** | Default smoke profile against the demo target (recruiter demo path). |
| `npm run example:load` | _Example_ | Illustrative load profile (ramping-vus, 5 VUs, ~2 min). |
| `npm run example:capacity` | _Example_ | Illustrative capacity profile (ramping-arrival-rate, find-the-ceiling, ~3 min). |
```

**package.json scripts order (D-13 recruiter-readability — supported first, examples second, raw third, infra last):**
```json
"scripts": {
  "build": "vite build",
  "build:watch": "vite build --watch",
  "validate:build": "node scripts/validate-build.mjs",
  "perf": "node scripts/perf-runner.mjs",
  "smoke": "node scripts/perf-runner.mjs --profile smoke --demo",
  "example:load": "node scripts/perf-runner.mjs --profile load --demo",
  "example:capacity": "node scripts/perf-runner.mjs --profile capacity --demo",
  "sync:src": "node scripts/sync-src.mjs",
  "convert-pages": "node scripts/convert-pages.mjs"
}
```

**Phase 03-02 deviation patterns ALREADY ENCODED in code (load.ts + capacity.ts must NOT re-introduce these defects):**
- D-01 no global URL → handled at `runtime-config.ts::normalizeBaseUrl` (don't author URL-using helpers in lib/simulations/lib/).
- D-02 explicit -e env flags → handled at `perf-runner.mjs::buildK6Args` (no change needed; flags already cover the 5 keys).
- D-04 `exec.test.abort` for SCEN-03 fail-fast → COPY VERBATIM from smoke.ts:71-78 into load.ts + capacity.ts default fns.
- D-03 PostPage body locator → scenarios untouched in this plan; carry-forward note from 03-02-SUMMARY only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author lib/simulations/load.ts + lib/simulations/capacity.ts + their RED-then-GREEN tests</name>
  <files>lib/simulations/load.ts, lib/simulations/capacity.ts, tests/unit/simulations-load.test.mjs, tests/unit/simulations-capacity.test.mjs</files>
  <read_first>
    - lib/simulations/smoke.ts (direct dispatch-shape analog — copy the default-function body verbatim, swap the announce-banner string and the makeHandleSummary profile key)
    - lib/simulations/lib/summary.ts (Plan 04-01 output — factory contract that load + capacity consume)
    - tests/unit/scenarios-registry.test.mjs::loadSmokeOptions (lines 124-180 — multi-source data-URL loader pattern; copy and adapt for load.ts and capacity.ts; the stubs object identical except for the file-path target)
    - .planning/phases/04-example-profiles-output-surface/04-CONTEXT.md (D-01..D-08 executor + threshold specs; D-14 EXAMPLE PROFILE banner; D-19 goja safety; 2026-05-11 amendment retargeting http_req_duration → browser_http_req_duration)
    - .planning/phases/04-example-profiles-output-surface/04-RESEARCH.md (§6 Examples 1 + 2 — verbatim load + capacity options literals; §5 Pitfall 1 — why browser_http_req_duration is the correct metric)
  </read_first>
  <behavior>
    Author RED tests first (load + capacity test files) then GREEN the implementations (load.ts + capacity.ts). Both simulation files are near-clones of smoke.ts with three differences each: (1) the EXAMPLE PROFILE banner per D-14, (2) the `options` literal (different executor + thresholds per CONTEXT), (3) the `profile: 'load'` / `profile: 'capacity'` factory key.

    Test files use the multi-source data-URL pattern from scenarios-registry.test.mjs::loadSmokeOptions. The stubs are IDENTICAL between load + capacity tests (and identical to the existing smoke loader stubs minus filename) — same 6 specifiers (k6/browser, k6/execution, @lib/scenarios, @pages/base/selectors, @config, ./lib/summary).

    Load test assertions (≥ 5 tests):
    - "load options: ramping-vus executor with 3 stages [30s→5, 60s@5, 30s→0]"
    - "load options: chromium browser type"
    - "load options: gracefulRampDown=30s and startVUs=0"
    - "load options: D-03 amended thresholds — browser_web_vital_lcp p(95)<4000"
    - "load options: D-03 amended threshold — browser_http_req_duration p(95)<2000 (NOT http_req_duration)" ← the critical landmine check
    - "load options: all 4 thresholds present (browser_web_vital_lcp, http_req_failed, iteration_duration, browser_http_req_duration)"
    - "load options: thresholds map does NOT contain key 'http_req_duration'" (negative-space check — verbatim guard against the pre-amendment string)

    Capacity test assertions (≥ 5 tests):
    - "capacity options: ramping-arrival-rate executor"
    - "capacity options: startRate=0, timeUnit='1s', preAllocatedVUs=10, maxVUs=10"
    - "capacity options: single 180s stage targeting 10"
    - "capacity options: chromium browser type"
    - "capacity options: D-08 amended thresholds — browser_http_req_duration p(95)<3000"
    - "capacity options: all 4 thresholds present"
    - "capacity options: thresholds map does NOT contain key 'http_req_duration'"
  </behavior>
  <action>
    **Step 1 — RED test files (commit FIRST, before implementation):**

    Create `tests/unit/simulations-load.test.mjs` (~110 lines):
    - Copy the `loadSmokeOptions` helper from `tests/unit/scenarios-registry.test.mjs:124-180` and rename to `loadLoadOptions`. Change the `smokePath` constant to `path.join(projectRoot, 'lib', 'simulations', 'load.ts')`.
    - The `stubs` object is IDENTICAL (6 keys: `k6/browser`, `k6/execution`, `@lib/scenarios`, `@pages/base/selectors`, `@config`, `./lib/summary` — same content).
    - Write the 7 test assertions per the behavior block. Critical assertions (must be verbatim):
      ```javascript
      assert.deepEqual(options.thresholds.browser_http_req_duration, ['p(95)<2000']);
      assert.equal(options.thresholds.http_req_duration, undefined, 'D-03 amendment: load uses browser_http_req_duration NOT http_req_duration');
      assert.deepEqual(options.scenarios.browser.stages, [
        { duration: '30s', target: 5 },
        { duration: '60s', target: 5 },
        { duration: '30s', target: 0 },
      ]);
      assert.equal(options.scenarios.browser.executor, 'ramping-vus');
      ```

    Create `tests/unit/simulations-capacity.test.mjs` (~110 lines):
    - Same loader pattern, target `lib/simulations/capacity.ts`. Rename helper to `loadCapacityOptions`.
    - The 7 capacity assertions. Critical:
      ```javascript
      assert.deepEqual(options.thresholds.browser_http_req_duration, ['p(95)<3000']);
      assert.equal(options.thresholds.http_req_duration, undefined, 'D-08 amendment: capacity uses browser_http_req_duration NOT http_req_duration');
      assert.equal(options.scenarios.browser.executor, 'ramping-arrival-rate');
      assert.equal(options.scenarios.browser.startRate, 0);
      assert.equal(options.scenarios.browser.timeUnit, '1s');
      assert.equal(options.scenarios.browser.preAllocatedVUs, 10);
      assert.equal(options.scenarios.browser.maxVUs, 10);
      assert.deepEqual(options.scenarios.browser.stages, [
        { duration: '180s', target: 10 },
      ]);
      ```

    Run `node --test tests/unit/simulations-load.test.mjs tests/unit/simulations-capacity.test.mjs` — both MUST fail with ENOENT on the not-yet-existing source files. Commit: `test(04-02): add RED tests for load + capacity simulation entries [PROF-02 PROF-03 amended D-03 D-08]`

    **Step 2 — GREEN implementation:**

    Create `lib/simulations/load.ts` (~95 lines). Copy `lib/simulations/smoke.ts` byte-for-byte to a working buffer, then apply three transformations:

    (a) Insert the D-14 banner as the very first line of the file (before any imports):
    ```typescript
    /**
     * EXAMPLE PROFILE — load testing shape.
     * Smoke is the supported demo path; this profile is illustrative of how
     * the same architecture (registry, runner, summary) scales beyond smoke.
     * See README §Profiles.
     */
    ```

    (b) Replace the `options` literal block VERBATIM with:
    ```typescript
    /**
     * Load profile options (D-01..D-04).
     *
     * Executor: ramping-vus, classic 3-stage shape (D-01) — peak 5 concurrent VUs,
     * total ~2 minutes wall time. Recruiter-recognized load shape.
     *
     * Thresholds (D-03 amended 2026-05-11):
     *   - browser_web_vital_lcp p(95)<4000  (concurrent chromium depresses LCP)
     *   - http_req_failed       rate<0.05    (sub-resource flake amplifies at 5 VUs)
     *   - iteration_duration    p(95)<25000  (concurrent journeys take longer)
     *   - browser_http_req_duration p(95)<2000  (load-relevant chromium latency signal —
     *     RETARGETED from http_req_duration per RESEARCH §5 Pitfall 1 + CONTEXT 2026-05-11
     *     amendment: k6/browser routes HTTP through chromium, leaving http_req_*
     *     empty per Phase 03-02 SUMMARY Run 1 evidence)
     *
     * Soft thresholds (D-04): no `abortOnFail` — trip is summarized, ramp completes.
     */
    export const options = {
      scenarios: {
        browser: {
          executor: 'ramping-vus',
          startVUs: 0,
          stages: [
            { duration: '30s', target: 5 },
            { duration: '60s', target: 5 },
            { duration: '30s', target: 0 },
          ],
          gracefulRampDown: '30s',
          options: {
            browser: { type: 'chromium' },
          },
        },
      },
      thresholds: {
        browser_web_vital_lcp: ['p(95)<4000'],
        http_req_failed: ['rate<0.05'],
        iteration_duration: ['p(95)<25000'],
        browser_http_req_duration: ['p(95)<2000'],
      },
    };
    ```

    (c) Replace the `export const handleSummary = makeHandleSummary({ profile: 'smoke', ...})` block with `profile: 'load'` (everything else identical). The default function body is copied VERBATIM from smoke.ts EXCEPT change the announce banner line from `[easyk6] smoke scenario=...` to `[easyk6] load scenario=...` AND rename the function from `smokeSimulation` to `loadSimulation`.

    Create `lib/simulations/capacity.ts` (~95 lines) the same way:

    (a) D-14 banner with "capacity testing shape" instead of "load testing shape".

    (b) options literal:
    ```typescript
    /**
     * Capacity profile options (D-05..D-08).
     *
     * Semantics (D-05): find-the-breaking-point, NOT sustain-the-target. PASS
     * in the recruiter narrative = "we discovered the ceiling" — a soft threshold
     * trip IS the signal, not a failure.
     *
     * Executor (D-06): ramping-arrival-rate — iter/s ramp decouples throughput
     * from chromium spin-up cost. Throughput is the X axis.
     *
     * Stage (D-07): 0 → 10 iter/s over 180s. preAllocatedVUs=maxVUs=10 caps the
     * VU pool — RESEARCH §5 Pitfall 6 documents the intentional ceiling.
     *
     * Thresholds (D-08 amended 2026-05-11):
     *   - browser_web_vital_lcp     p(95)<4000
     *   - http_req_failed           rate<0.05
     *   - browser_http_req_duration p(95)<3000  (RETARGETED from http_req_duration)
     *   - iteration_duration        p(95)<30000
     *
     * Soft thresholds (D-08): full ramp completes; the report narrates which
     * thresholds tripped at which point.
     */
    export const options = {
      scenarios: {
        browser: {
          executor: 'ramping-arrival-rate',
          startRate: 0,
          timeUnit: '1s',
          preAllocatedVUs: 10,
          maxVUs: 10,
          stages: [
            { duration: '180s', target: 10 },
          ],
          options: {
            browser: { type: 'chromium' },
          },
        },
      },
      thresholds: {
        browser_web_vital_lcp: ['p(95)<4000'],
        http_req_failed: ['rate<0.05'],
        browser_http_req_duration: ['p(95)<3000'],
        iteration_duration: ['p(95)<30000'],
      },
    };
    ```

    (c) `profile: 'capacity'` in the factory call. Default function renamed to `capacitySimulation`, announce banner `[easyk6] capacity scenario=...`.

    Run `node --test tests/unit/simulations-load.test.mjs tests/unit/simulations-capacity.test.mjs` — both must now be GREEN (14 tests across the two files).

    **Commit message:** `feat(04-02): add load.ts + capacity.ts example simulation entries [PROF-02 PROF-03 amended D-03 D-08]`
  </action>
  <verify>
    <automated>node --test tests/unit/simulations-load.test.mjs tests/unit/simulations-capacity.test.mjs && npm test</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/simulations/load.ts` exists with the EXAMPLE PROFILE banner — grep `EXAMPLE PROFILE — load testing shape` returns 1 match.
    - File `lib/simulations/capacity.ts` exists with the EXAMPLE PROFILE banner — grep `EXAMPLE PROFILE — capacity testing shape` returns 1 match.
    - `lib/simulations/load.ts` contains the literal string `'ramping-vus'` and the 3-stage block.
    - `lib/simulations/load.ts` contains EXACTLY `browser_http_req_duration: ['p(95)<2000']` (grep returns 1).
    - `lib/simulations/load.ts` contains the literal `makeHandleSummary({` followed by `profile: 'load'` on a subsequent line.
    - `lib/simulations/load.ts` contains the Q3 landmine fix `await page.goto(runtimeConfig.baseUrl)` (grep returns 1).
    - `lib/simulations/load.ts` contains `exec.test.abort(message)` (Phase 03-02 D-04 deviation pattern preserved).
    - `lib/simulations/capacity.ts` contains `'ramping-arrival-rate'`, `preAllocatedVUs: 10`, `maxVUs: 10`, `{ duration: '180s', target: 10 }`.
    - `lib/simulations/capacity.ts` contains EXACTLY `browser_http_req_duration: ['p(95)<3000']`.
    - `lib/simulations/capacity.ts` contains `profile: 'capacity'`.
    - **Anti-regression grep:** `grep -n "http_req_duration:" lib/simulations/load.ts lib/simulations/capacity.ts` returns ONLY lines with `browser_http_req_duration:` (zero matches for bare `http_req_duration:` followed by `[`). This proves the 2026-05-11 amendment is honored.
    - `lib/simulations/smoke.ts` is UNCHANGED in this task — grep verifies its `executor: 'shared-iterations'` is still present.
    - `tests/unit/simulations-load.test.mjs` exists with ≥ 7 test assertions; `node --test tests/unit/simulations-load.test.mjs` exits 0.
    - `tests/unit/simulations-capacity.test.mjs` exists with ≥ 7 test assertions; `node --test tests/unit/simulations-capacity.test.mjs` exits 0.
    - Negative-space assertion present in BOTH test files: `assert.equal(options.thresholds.http_req_duration, undefined, ...)` — grep `http_req_duration, undefined` returns 2 matches across the two files.
    - `npm test` total green; test count Phase-3 baseline 84 + Plan 04-01's 12 + Plan 04-02 Task 1's 14 = ~110 tests.
    - Two atomic commits landed: (1) RED tests, (2) GREEN implementation.
  </acceptance_criteria>
  <done>load.ts + capacity.ts authored with amended threshold strings + EXAMPLE PROFILE banner + Q3 fix + exec.test.abort fail-fast; 14 new tests green; smoke.ts untouched; both commits landed.</done>
</task>

<task type="auto">
  <name>Task 2: package.json scripts, validate-build.mjs strict list, runtime-config.ts D-18 cleanup, perf-runner tests</name>
  <files>package.json, scripts/validate-build.mjs, lib/config/runtime-config.ts, tests/unit/perf-runner.test.mjs</files>
  <read_first>
    - package.json (current `scripts` block — preserve all 7 existing entries; the order matters for recruiter readability per D-13)
    - scripts/validate-build.mjs (current 4-entry requiredFiles list — extend, do not replace)
    - lib/config/runtime-config.ts (current lines 5-12 — D-18 deletes the PHASE_ONE_SMOKE_ENTRY_FILE constant AND its 5-line JSDoc preamble; SMOKE_ENTRY_FILE retained on its own line)
    - tests/unit/perf-runner.test.mjs (existing 2 tests are untouched; this task ADDS 2 new tests for --profile load and --profile capacity dry-run)
    - tests/unit/runtime-config.test.mjs (verify it does NOT import PHASE_ONE_SMOKE_ENTRY_FILE — grep confirms it only imports SMOKE_ENTRY_FILE so D-18 cleanup breaks nothing)
    - .planning/phases/04-example-profiles-output-surface/04-CONTEXT.md (D-13 npm script ordering; D-17(f) strict validate-build recommendation; D-18 PHASE_ONE constant removal)
  </read_first>
  <action>
    **Step 1 — package.json scripts:**

    Edit `package.json`. The `scripts` block is currently:
    ```json
    "scripts": {
      "build": "vite build",
      "build:watch": "vite build --watch",
      "validate:build": "node scripts/validate-build.mjs",
      "perf": "node scripts/perf-runner.mjs",
      "smoke": "node scripts/perf-runner.mjs --profile smoke --demo",
      "sync:src": "node scripts/sync-src.mjs",
      "convert-pages": "node scripts/convert-pages.mjs"
    }
    ```

    Add TWO entries between `smoke` and `sync:src` (preserving D-13's recruiter-readable ordering — supported first, examples second, infra third):
    ```json
    "scripts": {
      "build": "vite build",
      "build:watch": "vite build --watch",
      "validate:build": "node scripts/validate-build.mjs",
      "perf": "node scripts/perf-runner.mjs",
      "smoke": "node scripts/perf-runner.mjs --profile smoke --demo",
      "example:load": "node scripts/perf-runner.mjs --profile load --demo",
      "example:capacity": "node scripts/perf-runner.mjs --profile capacity --demo",
      "sync:src": "node scripts/sync-src.mjs",
      "convert-pages": "node scripts/convert-pages.mjs"
    }
    ```

    Do NOT add `clean:reports` (CONTEXT discretion item — leave out for now). Do NOT modify any other top-level package.json key.

    **Step 2 — scripts/validate-build.mjs strict extension (D-17(f)):**

    Edit `scripts/validate-build.mjs`. Replace the `requiredFiles` array (currently 4 entries) with the strict 6-entry list:
    ```javascript
    const requiredFiles = [
      'dist/simulations/smoke.js',
      'dist/simulations/load.js',
      'dist/simulations/capacity.js',
      'scripts/perf-runner.mjs',
      '.env.example',
      'lib/config/runtime-config.ts',
    ];
    ```

    Keep everything else in the file unchanged (the `existsSync` filter loop, the error message, the success log).

    **Step 3 — lib/config/runtime-config.ts D-18 cleanup:**

    Edit `lib/config/runtime-config.ts`. Delete lines 5-12 inclusive (the JSDoc preamble + the `PHASE_ONE_SMOKE_ENTRY_FILE` export). Specifically remove:
    ```typescript
    /**
     * SMOKE_ENTRY_FILE is the canonical Phase 3 entry path (D-62).
     * PHASE_ONE_SMOKE_ENTRY_FILE remains exported only as a transition-period
     * alias consumed by validate-build.mjs until Plan 03-02 deletes the Phase 1
     * shell. Do NOT introduce new consumers of PHASE_ONE_SMOKE_ENTRY_FILE.
     */
    export const SMOKE_ENTRY_FILE = 'dist/simulations/smoke.js';
    export const PHASE_ONE_SMOKE_ENTRY_FILE = 'dist/tests/smoke/smoke-shell.test.js';
    ```

    Replace with a one-line `SMOKE_ENTRY_FILE` retention (no preamble — the constant is self-explanatory now that the transition is over):
    ```typescript
    export const SMOKE_ENTRY_FILE = 'dist/simulations/smoke.js';
    ```

    The resulting top of the file should be:
    ```typescript
    export const DEMO_BASE_URL = 'https://othrondir.github.io/QAbbalah/';
    export const DEFAULT_ENV_FILE = '.env';
    export const DEFAULT_PROFILE = 'smoke';
    export const DEFAULT_SCENARIO = 'home-smoke';
    export const SMOKE_ENTRY_FILE = 'dist/simulations/smoke.js';

    export const RUNTIME_FLAG_DEFINITIONS = [ ... ];
    ```

    **Step 4 — tests/unit/perf-runner.test.mjs extension:**

    Append TWO new test cases at the end of the existing file (after the existing 2 tests; preserve all existing imports, helpers, and tests). Insert before the final closing of the module:
    ```javascript
    test('perf-runner: --profile load --dry-run resolves to dist/simulations/load.js', () => {
      const env = { ...process.env };
      delete env.K6_PROFILE;
      delete env.K6_SCENARIO;
      const result = runRunner(['--profile', 'load', '--demo', '--dry-run'], env);
      assert.equal(result.status, 0, result.stderr || result.stdout);
      assert.match(result.stdout, /k6 run.*-e SCENARIO=home-smoke.*dist\/simulations\/load\.js/u);
      assert.match(result.stdout, /Resolved base URL: https:\/\/othrondir\.github\.io\/QAbbalah\//u);
    });

    test('perf-runner: --profile capacity --dry-run resolves to dist/simulations/capacity.js', () => {
      const env = { ...process.env };
      delete env.K6_PROFILE;
      delete env.K6_SCENARIO;
      const result = runRunner(['--profile', 'capacity', '--demo', '--dry-run'], env);
      assert.equal(result.status, 0, result.stderr || result.stdout);
      assert.match(result.stdout, /k6 run.*-e SCENARIO=home-smoke.*dist\/simulations\/capacity\.js/u);
    });
    ```

    **Step 5 — Green-bar gate:**

    Run the full gate:
    ```
    npm run build           # must emit smoke.js + load.js + capacity.js
    npm run validate:build  # must pass strict 6-file list
    npm test                # all tests including the two new perf-runner cases
    ```

    All three must exit 0. If `npm run build` does NOT emit `dist/simulations/load.js` or `dist/simulations/capacity.js`, investigate: Vite glob at `vite.config.ts:32` should auto-pick them — confirm the new files match `./lib/simulations/**/*.ts` and the export shape is consistent with smoke.ts.

    **Commit message:** `feat(04-02): add example:load/capacity scripts, strict validate-build, drop PHASE_ONE_SMOKE_ENTRY_FILE [PROF-02 PROF-03 D-13 D-17(f) D-18]`
  </action>
  <verify>
    <automated>grep -rE "PHASE_ONE_SMOKE_ENTRY_FILE" lib scripts tests | head -5 ; npm test && npm run build && npm run validate:build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `scripts.example:load` exists and equals `"node scripts/perf-runner.mjs --profile load --demo"` (grep returns 1).
    - `package.json` `scripts.example:capacity` exists and equals `"node scripts/perf-runner.mjs --profile capacity --demo"` (grep returns 1).
    - `package.json` `scripts.smoke` UNCHANGED — still equals `"node scripts/perf-runner.mjs --profile smoke --demo"`.
    - `package.json` does NOT introduce a `clean:reports` key (CONTEXT discretion — out of scope).
    - `scripts/validate-build.mjs` `requiredFiles` array length is exactly 6.
    - `scripts/validate-build.mjs` contains the literal `'dist/simulations/load.js'` (grep returns 1) and `'dist/simulations/capacity.js'` (grep returns 1).
    - `scripts/validate-build.mjs` does NOT contain `dist/tests/smoke/smoke-shell.test.js` (Phase 1 shell — deleted in Plan 03-02 Task 3) — grep returns 0.
    - `lib/config/runtime-config.ts` does NOT contain `PHASE_ONE_SMOKE_ENTRY_FILE` — `grep -c PHASE_ONE_SMOKE_ENTRY_FILE lib/config/runtime-config.ts` returns 0.
    - **Global grep audit:** `grep -rE "PHASE_ONE_SMOKE_ENTRY_FILE" lib scripts tests .planning/phases/04-example-profiles-output-surface | wc -l` returns 0 (or only matches inside the 04-CONTEXT.md / 04-RESEARCH.md / 04-PATTERNS.md docs which are reference material — verify no `lib/`, `scripts/`, `tests/` matches).
    - `lib/config/runtime-config.ts` still exports `SMOKE_ENTRY_FILE` — grep `export const SMOKE_ENTRY_FILE` returns 1.
    - `tests/unit/perf-runner.test.mjs` contains the literal `dist/simulations/load.js` (new test assertion) and `dist/simulations/capacity.js` (grep returns ≥1 each).
    - Existing perf-runner tests untouched — the two tests `shell-provided BASE_URL is accepted by the public runner` and `env file beats shell BASE_URL in the public runner` still present and green.
    - `npm test` total exit 0 with all tests passing (test count: ~110 → ~112 after perf-runner extension).
    - `npm run build` exits 0 AND emits all three simulation outputs. Verify: `ls dist/simulations/smoke.js dist/simulations/load.js dist/simulations/capacity.js` returns all three.
    - `npm run validate:build` exits 0 with the strict 6-file message: `Validated build shell: dist/simulations/smoke.js, dist/simulations/load.js, dist/simulations/capacity.js, scripts/perf-runner.mjs, .env.example, lib/config/runtime-config.ts`.
  </acceptance_criteria>
  <done>4 surface edits landed; PHASE_ONE_SMOKE_ENTRY_FILE eliminated; build emits 3 simulations; validate-build strict; perf-runner test coverage extended; commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: README quickstart Supported-vs-Example table (D-13 + D-15 contract)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current structure — sections at lines 7, 41, 53, 72, 92, 96; the table lands in or above the `## Commands` section per D-15 "directly after the project intro, before install/build steps")
    - .planning/phases/04-example-profiles-output-surface/04-CONTEXT.md (D-13 table contract — 3 rows verbatim; D-15 placement requirement)
    - .planning/phases/04-example-profiles-output-surface/04-RESEARCH.md (§6 Example 6 — package.json scripts ordering rationale; same recruiter-narrative applies to README table)
  </read_first>
  <action>
    Edit `README.md`. Insert a new `## Quickstart` section between the existing intro paragraphs (lines 1-6) and the existing `## Architecture First` section (line 7). The new section is short — table contract only; Phase 5 (DOCS-01/DOCS-02) owns the surrounding prose polish.

    Insert this block (use Edit tool to add after the line `Phase 1 establishes the build foundation, repo boundaries, and the shared runtime-config contract behind \`npm run smoke\` and \`npm run perf\`.`):

    ```markdown

    ## Quickstart

    Smoke is the supported demo path; load and capacity are illustrative examples sharing the same architecture.

    | Command | Status | What it does |
    |---|---|---|
    | `npm run smoke` | **Supported** | Default smoke profile against the demo target (recruiter demo path). |
    | `npm run example:load` | _Example_ | Illustrative load profile (ramping-vus, 5 VUs, ~2 min). |
    | `npm run example:capacity` | _Example_ | Illustrative capacity profile (ramping-arrival-rate, find-the-ceiling, ~3 min). |

    All three profiles write recruiter-readable artifacts to `reports/<profile>-<scenario>.md` + `reports/<profile>-<scenario>.json` (gitignored).

    ```

    Do NOT modify any other README section. Phase 5 will overhaul the surrounding prose (DOCS-01, DOCS-02) — Phase 4 only lands the table contract.
  </action>
  <verify>
    <automated>grep -c "\\*\\*Supported\\*\\*" README.md && grep -c "_Example_" README.md && grep -c "npm run example:load" README.md && grep -c "npm run example:capacity" README.md && grep -c "## Quickstart" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains exactly one occurrence of the literal string `## Quickstart` (the new section header).
    - `README.md` contains the literal `**Supported**` (matches the table row marker — grep returns ≥1).
    - `README.md` contains the literal `_Example_` twice (one for load, one for capacity — grep returns ≥2).
    - `README.md` contains the literal `npm run smoke` in a table-row context (within a pipe-delimited line).
    - `README.md` contains the literal `npm run example:load` (grep returns ≥1).
    - `README.md` contains the literal `npm run example:capacity` (grep returns ≥1).
    - `README.md` contains the literal `| Command | Status | What it does |` (header row — grep returns 1).
    - `README.md` mentions `reports/<profile>-<scenario>.md` (footer line — grep returns 1).
    - All existing README sections still present: `## Architecture First`, `## Commands`, `## Upstream Reuse`, `## Runtime Config`, `## Legacy Note`, `## Next Reference` — grep `^## ` returns ≥7 section headers.
    - No prose changes outside the new `## Quickstart` block (Phase 5 owns prose polish).
  </acceptance_criteria>
  <done>3-row Supported-vs-Example table lands in README Quickstart section; existing sections preserved; table contract is recruiter-readable; commit landed (`docs(04-02): add Quickstart Supported-vs-Example table contract [D-13 D-15]`).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (verify wave): Capture real-run recruiter evidence — smoke + load + capacity vs QAbbalah</name>
  <what-built>
    Plan 04-01 + Plan 04-02 Tasks 1-3 together deliver:
    - Three k6 simulation entries (smoke retuned through shared factory; load and capacity new examples) all writing reports/&lt;profile&gt;-&lt;scenario&gt;.md + .json on every run via makeHandleSummary
    - Two new npm scripts (example:load, example:capacity) labeled distinctly from the supported `npm run smoke` path
    - Strict validate-build covering all three dist outputs
    - README Quickstart Supported-vs-Example table contract
    - PHASE_ONE_SMOKE_ENTRY_FILE constant cleanup (D-18)

    All automated tests are green at this point; this checkpoint captures the REAL-RUN evidence that closes PROF-02 + PROF-03 + PROF-04 end-to-end.
  </what-built>
  <how-to-verify>
    Execute three real k6 runs against the locked demo target https://othrondir.github.io/QAbbalah/ from a clean working tree (after `rm -rf reports/` to prove every run regenerates its artifacts):

    **Step 1 — clean workspace:**
    ```powershell
    Remove-Item -Recurse -Force reports -ErrorAction SilentlyContinue
    npm run build
    ```
    Expected: build exits 0; `dist/simulations/{smoke,load,capacity}.js` all exist.

    **Step 2 — smoke regression run (PROF-04 supported-path evidence):**
    ```powershell
    npm run smoke
    ```
    Expected:
    - Exit code 0.
    - Stdout banner `Resolved base URL: https://othrondir.github.io/QAbbalah/` followed by the `k6 run -e SCENARIO=home-smoke ... dist/simulations/smoke.js` command line.
    - All 3 D-66 thresholds pass (browser_web_vital_lcp p(95)<3000, http_req_failed rate<0.01, iteration_duration p(95)<15000).
    - Filesystem after run: `reports/smoke-home-smoke.md` AND `reports/smoke-home-smoke.json` both exist.
    - Markdown opens cleanly on GitHub web UI; reads top-to-bottom in ≤30 seconds; thresholds table shows ✅ PASS for the 3 smoke thresholds; browser_http_req_duration row shows a real ms value (NOT n/a — Phase 03-02 Run 1 had real samples even in smoke).

    **Step 3 — load example run (PROF-02 evidence):**
    ```powershell
    npm run example:load -- --demo
    ```
    Expected:
    - Exit code 0 (or 99 if a soft threshold trips — both valid per D-04).
    - Banner shows `k6 run -e SCENARIO=home-smoke ... -e K6_PROFILE=load ... dist/simulations/load.js`.
    - Console announce: `[easyk6] load scenario=home-smoke profile=load baseUrl=https://othrondir.github.io/QAbbalah/`.
    - Full 2-minute ramp completes (30s → 5 VUs → 60s hold → 30s ramp down).
    - All 4 D-03 amended thresholds appear in the k6 stdout summary, including `browser_http_req_duration p(95)<2000`.
    - Filesystem: `reports/load-home-smoke.md` + `reports/load-home-smoke.json` exist.
    - Markdown's threshold table includes a row for `browser_http_req_duration` with a real ms value (this is the empirical validation that the 2026-05-11 amendment was the right call — RESEARCH §5 Pitfall 1).

    **Step 4 — capacity example run (PROF-03 evidence):**
    ```powershell
    npm run example:capacity -- --demo
    ```
    Expected:
    - Exit code 0 or 99 (soft trip is the recruiter narrative per D-05).
    - Banner shows `dist/simulations/capacity.js`.
    - Console announce: `[easyk6] capacity scenario=home-smoke profile=capacity baseUrl=...`.
    - Full 3-minute ramp completes.
    - Markdown's threshold table likely shows at least one ❌ FAIL on `iteration_duration p(95)<30000` near the ceiling (the desired "find-the-breaking-point" narrative).
    - Filesystem: `reports/capacity-home-smoke.md` + `reports/capacity-home-smoke.json` exist.

    **Step 5 — calibration call (CONTEXT §Specifics):**
    If the capacity run does NOT trip ANY threshold against QAbbalah's CDN-cached steady state, CONTEXT §Specifics permits bumping the peak rate or duration. Decisions:
    - If the run trips ≥1 threshold → keep D-07 stages verbatim, capture evidence as-is.
    - If the run trips ZERO thresholds AND `iteration_duration p(95)` is comfortably under 30000ms → log a deviation in 04-02-SUMMARY.md `deviations:` block, bump the stage `target: 10` to `target: 15` (or duration `'180s'` to `'240s'`), rerun, and capture the new evidence. Note both runs in the SUMMARY.

    **Step 6 — capture evidence:**
    Capture the stdout from all three runs and the full markdown contents of all 6 report files into `.planning/phases/04-example-profiles-output-surface/04-02-SUMMARY.md` per the Phase 03-02 SUMMARY precedent (3 captured runs, threshold rows verbatim, exit codes, deviation log entries if any landed).

    **Step 7 — final green-bar gate (post-evidence):**
    ```powershell
    npm test
    npm run build
    npm run validate:build
    ```
    All exit 0. The 6 report files in `reports/` are gitignored — they do not change the git status of this commit.
  </how-to-verify>
  <resume-signal>Type `approved` after attaching the three captured runs + the 6 markdown artifact filenames in 04-02-SUMMARY.md, OR describe issues (failed thresholds beyond the soft-trip narrative, missing report files, k6 runtime errors, deviation findings that require CONTEXT amendment).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer laptop ↔ external demo target | k6 browser scenarios spin up chromium and load https://othrondir.github.io/QAbbalah/ — third-party GitHub Pages content. |
| `__ENV.SCENARIO` (CLI) → reports/&lt;...&gt;-&lt;scenario&gt;.md path key | CLI-supplied scenario id flows through `makeHandleSummary` (Plan 04-01) into a filesystem path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Denial of Service | `lib/simulations/capacity.ts` ramping-arrival-rate at maxVUs=10 against a developer laptop | accept | D-07 caps RAM ceiling at ~3 GB peak (16 GB laptop friendly). Worst case the executor falls behind (RESEARCH §5 Pitfall 6) — this IS the recruiter narrative (D-05 find-the-breaking-point). Bounded by design. |
| T-04-02-02 | Information Disclosure | `reports/<profile>-<scenario>.json` writes the full k6 `data` payload including any user-agent / cookie / IP details k6 captured | accept | `reports/` is gitignored (line 37 of .gitignore). No PII against the QAbbalah demo target. JSON sibling is the documented raw-data path (D-09). |
| T-04-02-03 | Tampering | Regression: load.ts or capacity.ts could accidentally include `http_req_duration:` (pre-amendment string) and the threshold would trivially pass against browser scenarios | mitigate | Task 1 acceptance criteria include explicit anti-regression grep (`grep -n "http_req_duration:" lib/simulations/load.ts lib/simulations/capacity.ts` returns ONLY `browser_http_req_duration:` matches). Tests in `simulations-load.test.mjs` + `simulations-capacity.test.mjs` include negative-space assertions (`assert.equal(options.thresholds.http_req_duration, undefined, ...)`). |
| T-04-02-04 | Spoofing | A recruiter cloning the repo could read the `## Quickstart` table and assume load/capacity are first-class supported workflows | mitigate | Three orthogonal example-labeling signals per D-13/D-14/D-15: (1) package.json script prefix `example:`, (2) JSDoc EXAMPLE PROFILE banner in load.ts + capacity.ts, (3) README table marks load/capacity as `_Example_` (italicized) vs smoke as `**Supported**` (bold). |
| T-04-02-05 | Tampering | The Q3 landmine (HomePage.pageUrl='' → K6Page.navigate() short-circuits, scenario strands on blank page) could be re-introduced if load.ts / capacity.ts skip the explicit `await page.goto(runtimeConfig.baseUrl)` line | mitigate | Task 1 acceptance criteria mandate grep verification that BOTH new entries contain the literal `await page.goto(runtimeConfig.baseUrl)` line BEFORE `await entry.fn(...)`. Phase 03-01 D-A7 documented; Phase 04 must not regress. |
</threat_model>

<verification>
After Task 4 approval, run the full phase-completion gate:

1. **Automated suite — total green:**
   ```
   npm test
   ```
   Expected: ~112 tests pass, exit 0.

2. **Build + strict validate:**
   ```
   npm run build && npm run validate:build
   ```
   Expected: 3 simulation outputs emitted, strict 6-file list validated.

3. **Anti-regression — amended threshold strings:**
   ```
   grep -n "http_req_duration" lib/simulations/load.ts lib/simulations/capacity.ts
   ```
   Expected: ONLY occurrences are `browser_http_req_duration:` (prefix). Bare `http_req_duration:` followed by `[` returns ZERO matches.

4. **Anti-regression — PHASE_ONE_SMOKE_ENTRY_FILE eliminated:**
   ```
   grep -rE "PHASE_ONE_SMOKE_ENTRY_FILE" lib scripts tests
   ```
   Expected: zero matches.

5. **Anti-regression — Q3 landmine fix preserved in both new entries:**
   ```
   grep -n "page.goto(runtimeConfig.baseUrl)" lib/simulations/smoke.ts lib/simulations/load.ts lib/simulations/capacity.ts
   ```
   Expected: 3 matches (one per file).

6. **Anti-regression — exec.test.abort fail-fast preserved in all 3 entries:**
   ```
   grep -n "exec.test.abort" lib/simulations/smoke.ts lib/simulations/load.ts lib/simulations/capacity.ts
   ```
   Expected: 3 matches (one per file).

7. **README table contract present:**
   ```
   grep -c "**Supported**\|_Example_" README.md
   ```
   Expected: ≥3 matches (1 Supported + 2 Example).

8. **6 real-run artifacts captured in 04-02-SUMMARY.md:**
   - reports/smoke-home-smoke.md (smoke regression)
   - reports/smoke-home-smoke.json
   - reports/load-home-smoke.md (PROF-02 evidence)
   - reports/load-home-smoke.json
   - reports/capacity-home-smoke.md (PROF-03 evidence)
   - reports/capacity-home-smoke.json
</verification>

<success_criteria>
Plan 04-02 ships when:

- [ ] `lib/simulations/load.ts` exists with EXAMPLE PROFILE banner, ramping-vus executor, 3-stage shape, chromium browser, and verbatim amended D-03 thresholds (including `browser_http_req_duration: ['p(95)<2000']`)
- [ ] `lib/simulations/capacity.ts` exists with EXAMPLE PROFILE banner, ramping-arrival-rate executor, D-07 stage parameters (preAllocatedVUs=10, maxVUs=10, 180s→10), chromium browser, and verbatim amended D-08 thresholds (including `browser_http_req_duration: ['p(95)<3000']`)
- [ ] Both new entries reuse the Phase 3 dispatch shape: resolveRuntimeConfig + SCENARIO_REGISTRY + exec.test.abort fail-fast + page.goto(runtimeConfig.baseUrl) Q3 fix + makeHandleSummary factory
- [ ] `tests/unit/simulations-load.test.mjs` + `tests/unit/simulations-capacity.test.mjs` exist with ≥7 assertions each including negative-space anti-regression checks (`http_req_duration` key MUST be undefined)
- [ ] `tests/unit/perf-runner.test.mjs` extended with --profile load + --profile capacity dry-run cases; existing 2 perf-runner tests still green
- [ ] `package.json` scripts has `example:load` + `example:capacity` in recruiter-readable order (after smoke, before sync:src)
- [ ] `scripts/validate-build.mjs::requiredFiles` is strict 6-file list including both new dist outputs
- [ ] `lib/config/runtime-config.ts` no longer exports `PHASE_ONE_SMOKE_ENTRY_FILE` (D-18 cleanup); SMOKE_ENTRY_FILE retained
- [ ] Global grep `PHASE_ONE_SMOKE_ENTRY_FILE` in lib/, scripts/, tests/ returns zero matches
- [ ] `README.md` Quickstart section has 3-row Supported-vs-Example table per D-13/D-15 verbatim
- [ ] Verify checkpoint: 3 real k6 runs captured against QAbbalah (smoke regression + load example + capacity example); 6 report artifacts written to `reports/`; evidence in 04-02-SUMMARY.md mirrors the Phase 03-02 SUMMARY precedent
- [ ] Final green-bar gate: `npm test && npm run build && npm run validate:build` exits 0
- [ ] All four Phase 4 ROADMAP success criteria observably TRUE:
  - SC1: Load profile code exists and reuses runner/config architecture ← lib/simulations/load.ts + zero runner/config changes
  - SC2: Capacity profile code exists and reuses runner/config architecture ← lib/simulations/capacity.ts + zero runner/config changes
  - SC3: Smoke execution emits a readable summary artifact ← reports/smoke-home-smoke.md captured in 04-02-SUMMARY
  - SC4: Example profiles are clearly labeled (not the main supported path) ← 3-signal labeling (npm script prefix + JSDoc banner + README table)
</success_criteria>

<output>
After Task 4 approval, create `.planning/phases/04-example-profiles-output-surface/04-02-SUMMARY.md` capturing:

- **Status table** — 4 tasks, complete state
- **Three captured real runs** — for smoke, load, capacity (mirror Phase 03-02 SUMMARY format):
  - Command, banner output, console announce
  - Exit code
  - Full k6 stdout summary (THRESHOLDS + TOTAL RESULTS + WEB_VITALS + BROWSER sections, trimmed of ANSI)
  - Filesystem verification — `reports/<profile>-home-smoke.md` + `.json` exist
  - Full markdown contents of each `.md` artifact (paste verbatim as code blocks)
- **Threshold evidence table** — actual values from each run mapped to the threshold strings:
  - Smoke (D-66 verbatim from Phase 3): 3 thresholds
  - Load (amended D-03): 4 thresholds; CRITICAL — browser_http_req_duration p(95) real ms value (proves the amendment was right)
  - Capacity (amended D-08): 4 thresholds; CRITICAL — note which trip (D-05 ceiling narrative)
- **Deviations** — any runtime quirks discovered during real runs (mirror Phase 03-02's 4-entry deviation log format)
- **Capacity calibration decision** — kept D-07 verbatim, or bumped per CONTEXT §Specifics rationale
- **Phase 4 ROADMAP success criteria** — 4-row table, all 4 marked observably TRUE
- **Carry-forward** — any items deferred to Phase 5 docs (e.g., k6 1.5 runtime caveats section in docs/DEVELOPMENT.md per Phase 03-02 carry-forward #2)
- **Phase 4 closure** — PROF-02, PROF-03, PROF-04 marked closed; ROADMAP + STATE.md ready to update post-approval
</output>
