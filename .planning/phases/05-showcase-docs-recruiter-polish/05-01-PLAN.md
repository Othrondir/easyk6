---
phase: 05-showcase-docs-recruiter-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ARCHITECTURE.md
  - README.md
  - QUICKSTART.md
  - CONTRIBUTING.md
  - PROJECT_STRUCTURE.md
  - package.json
  - CLAUDE.md
autonomous: true
requirements: [DOCS-01, DOCS-02]
tags: [docs, readme, architecture, recruiter-polish, milestone-close]
must_haves:
  truths:
    - "A recruiter opening the GitHub repo sees a README whose first 30 seconds explain the project, show the Quickstart Supported-vs-Example table, and point to ARCHITECTURE.md"
    - "A recruiter opening ARCHITECTURE.md sees five H2 sections in D-07 order that explain what was lifted from ir-perf-k6, what was deliberately rejected, the upstream-reuse pipeline, the k6 1.5 caveats encoded in code, and a decision-log index"
    - "The Phase 4 Quickstart Supported-vs-Example table renders byte-identical in the rewritten README (zero changes to the 11-line block at current README lines 7-17)"
    - "QUICKSTART.md, CONTRIBUTING.md, and PROJECT_STRUCTURE.md no longer exist at repo root after Phase 5"
    - "No README.md or CLAUDE.md line references QUICKSTART.md, CONTRIBUTING.md, or PROJECT_STRUCTURE.md after Phase 5"
    - "BUILD-02 / SCEN-02 / F-01 / F-02 are surfaced as honest carry-forward in at least one recruiter-visible doc (README and/or ARCHITECTURE.md)"
    - "The PROJECT_STRUCTURE.md boundary-label content (src/pages, lib/pages, lib/pages-k6-patches, legacy-js) appears in ARCHITECTURE.md §3 (Upstream reuse pipeline) with Phase-1-era 'next plan' parentheticals stripped"
    - "ARCHITECTURE.md §4 cites Phase 03-02 SUMMARY.md and Phase 04 RESEARCH §5 Pitfall 1 as evidence pointers (cite, do not re-prove)"
    - "ARCHITECTURE.md §5 Decision log indexes .planning/ as a pointer list, not a duplicate of STATE.md Decisions"
    - "Grafana / OTEL deferral is visible to a recruiter without scrolling into .planning/ (README and/or ARCHITECTURE.md §2)"
  artifacts:
    - path: "ARCHITECTURE.md"
      provides: "5-section design narrative at repo root (NEW per D-04 + D-07)"
      contains: "## Adapted from `ir-perf-k6`, ## Simplified on purpose, ## Upstream reuse pipeline, ## k6 1.5 runtime caveats, ## Decision log"
    - path: "README.md"
      provides: "Recruiter-facing top-level doc (FULL REWRITE per D-06; Phase 4 Quickstart table preserved verbatim)"
      contains: "## Quickstart, `npm run smoke`, `npm run example:load`, `npm run example:capacity`, `**Supported**`, `_Example_`"
    - path: "package.json"
      provides: "Manifest (optional `description` polish per Claude's Discretion — recruiter-tuned wording)"
      contains: "\"description\":"
  key_links:
    - from: "README.md"
      to: "ARCHITECTURE.md"
      via: "single-paragraph pointer under §Architecture or §Next Reference"
      pattern: "ARCHITECTURE\\.md"
    - from: "ARCHITECTURE.md §3"
      to: "PROJECT_STRUCTURE.md boundary content (folded)"
      via: "verbatim-or-close fold of the four boundary labels + sync-meta provenance"
      pattern: "src/pages.*lib/pages.*lib/pages-k6-patches.*legacy-js"
    - from: "ARCHITECTURE.md §4"
      to: ".planning/phases/03-smoke-scenarios-supported-execution/03-02-SUMMARY.md"
      via: "evidence citation for k6 1.5 caveats"
      pattern: "03-02-SUMMARY\\.md|7d629ba"
    - from: "ARCHITECTURE.md §5"
      to: ".planning/STATE.md / .planning/PROJECT.md / .planning/phases/**/CONTEXT.md"
      via: "decision-log pointer index"
      pattern: "\\.planning/"
---

<objective>
Deliver the v1 milestone-closing documentation surface: a recruiter-readable, locally-runnable repo whose top-level docs explain the upstream-reuse story, the architectural judgment, and the smoke-first scope without ambiguity. This is the Phase 5 doc rewrite + final polish pass. The Phase 4 Quickstart Supported-vs-Example table contract is locked and preserved verbatim; everything else in README is rewritten. ARCHITECTURE.md is authored fresh at repo root with five sections per D-07. QUICKSTART.md, CONTRIBUTING.md, and PROJECT_STRUCTURE.md are deleted (PROJECT_STRUCTURE.md content folds into ARCHITECTURE.md §3 first).

Purpose: Close DOCS-01 (README explains upstream reuse + smoke demo path) and DOCS-02 (architecture docs explain ir-perf-k6 adaptation and intentional simplifications), satisfying ROADMAP Phase 5 Success Criteria 1-4 and closing the v1 milestone.

Output: Two canonical docs at repo root (README.md rewritten, ARCHITECTURE.md new); three stale docs deleted; carry-forward (BUILD-02 / SCEN-02 / F-01 / F-02) surfaced honestly per D-09; optional polish on package.json `description` + CLAUDE.md Conventions/Architecture placeholders per Claude's Discretion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-showcase-docs-recruiter-polish/05-CONTEXT.md
@.planning/phases/05-showcase-docs-recruiter-polish/05-PATTERNS.md
@.planning/phases/03-smoke-scenarios-supported-execution/03-02-SUMMARY.md
@.planning/phases/04-example-profiles-output-surface/04-CONTEXT.md
@.planning/phases/04-example-profiles-output-surface/04-RESEARCH.md
@CLAUDE.md
@README.md
@PROJECT_STRUCTURE.md

<interfaces>
<!-- Verbatim-preservation contract from Phase 4 D-13/D-15 — paste EXACTLY these 11 lines into the rewritten README, including blank lines, bold/italic markers, exact command names, exact parenthetical descriptions. Any deviation is a Phase 4 contract break. Source: current README.md lines 7-17 + the trailing sentence at line 17. -->

Phase 4 Quickstart block (PRESERVE VERBATIM):

```markdown
## Quickstart

Smoke is the supported demo path; load and capacity are illustrative examples sharing the same architecture.

| Command | Status | What it does |
|---|---|---|
| `npm run smoke` | **Supported** | Default smoke profile against the demo target (recruiter demo path). |
| `npm run example:load` | _Example_ | Illustrative load profile (ramping-vus, 5 VUs, ~2 min). |
| `npm run example:capacity` | _Example_ | Illustrative capacity profile (ramping-arrival-rate, find-the-ceiling, ~3 min). |

All three profiles write recruiter-readable artifacts to `reports/<profile>-<scenario>.md` + `reports/<profile>-<scenario>.json` (gitignored).
```

<!-- Current repo-root file list (factual, captured 2026-05-12 — use this to regenerate the ARCHITECTURE.md §3 layout tree, NOT the stale PROJECT_STRUCTURE.md tree which still shows `k6/simulations/smoke/smoke-shell.test.ts`): -->

Repo root contents (top level):
- CLAUDE.md (maintainer tooling)
- CONTRIBUTING.md (DELETE per D-03)
- PROJECT_STRUCTURE.md (FOLD then DELETE per D-05)
- QUICKSTART.md (DELETE per D-02)
- README.md (REWRITE per D-06)
- dist/ (Vite build output; gitignored)
- k6/ (legacy Phase-1 shell directory; scenarios/.gitkeep + simulations/smoke/ now empty after Phase 03-02 deletion — confirm at execute time whether the empty `k6/` tree should appear in the new layout or be culled visually)
- legacy-js/ (archived starter — KEEP per D-08)
- lib/ — config/, pages/, pages-k6-patches/, scenarios/, simulations/, vendor/
- lib/simulations/ — capacity.ts, load.ts, smoke.ts, lib/ (summary/format-md/format-json)
- node_modules/ (gitignored)
- package-lock.json
- package.json
- reports/ (gitignored runtime output dir)
- scripts/ — convert-pages.mjs, lib/, perf-runner.mjs, sync-src.mjs, validate-build.mjs
- src/pages/ — AboutPage.ts, BasePage.ts, HomePage.ts, PostPage.ts, components/, index.ts, .sync-meta.json (when synced)
- tests/ (unit + integration test trees)
- tsconfig.json
- vite.config.ts

<!-- npm-script surface (package.json:6-16 — quote these EXACTLY in README §Commands; no paraphrasing of script names): -->

npm scripts available (verbatim from package.json):
- `npm run build` — `vite build`
- `npm run build:watch` — `vite build --watch`
- `npm run validate:build` — `node scripts/validate-build.mjs`
- `npm run perf` — `node scripts/perf-runner.mjs`
- `npm run smoke` — `node scripts/perf-runner.mjs --profile smoke --demo`
- `npm run example:load` — `node scripts/perf-runner.mjs --profile load --demo`
- `npm run example:capacity` — `node scripts/perf-runner.mjs --profile capacity --demo`
- `npm run sync:src` — `node scripts/sync-src.mjs`
- `npm run convert-pages` — `node scripts/convert-pages.mjs`

<!-- Carry-forward surfacing menu (D-09 — MUST appear in at least one recruiter-visible doc; planner chose option (c) "both" surfaces, distributed: README gets a light "Known carry-forward" mention; ARCHITECTURE.md §2 closes with a "Known limitations & deferred work" subsection. Tonal model: .planning/phases/03-smoke-scenarios-supported-execution/03-02-SUMMARY.md:347-358 carry-forward bullets — sober, concrete, pointer-driven.): -->

Carry-forward items (verbatim list — surface in docs, do NOT retroactively implement):
- **BUILD-02** — runtime config fail-fast on missing/invalid env (REQUIREMENTS.md:18 still `[ ]`). Phase 1 closed the foundation; the strict-validation closure was descoped pre-Phase-2.
- **SCEN-02** — smoke scenarios real-journey vs demo-target (REQUIREMENTS.md:23 still `[ ]`). Phase 03-02 captured real-run evidence against `https://othrondir.github.io/QAbbalah/` (exit 0, 3/3 D-66 thresholds pass); the checkbox is a hygiene gap, not missing work. Evidence: `.planning/phases/03-smoke-scenarios-supported-execution/03-02-SUMMARY.md`.
- **F-01** — capacity real-run evidence deferred from Plan 04-02 due to recruiter PC saturation after the load run. Static gates green (unit tests, build, validate, dry-run). Evidence path: `npm run example:capacity` against the demo target.
- **F-02** — smoke single-iteration LCP renders `n/a (no samples — browser scenario)` in Key Metrics row. Informational, non-blocking; surfaces when only one iteration sample fires.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ARCHITECTURE.md (NEW at repo root) with 5 sections per D-07, folding PROJECT_STRUCTURE.md content into §3</name>
  <files>ARCHITECTURE.md</files>
  <read_first>
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\phases\05-showcase-docs-recruiter-polish\05-CONTEXT.md (D-04, D-05, D-07, D-08, D-09 — section order + fold contract + legacy-js callout + carry-forward placement option (b))
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\phases\05-showcase-docs-recruiter-polish\05-PATTERNS.md (ARCHITECTURE.md Pattern Assignments §B-§F — section-by-section source-material map; Shared Patterns §Decision-log indexing + §Out-of-Scope honesty)
    - C:\Users\pzhly\Documents\GitHub\easyk6\PROJECT_STRUCTURE.md (full file — source for §3 fold; lines 7-38 Primary Layout tree, lines 40-49 Boundary Definitions, lines 52-90 Root Directories WHY narrative, lines 102-114 Sync Provenance table — refresh Phase-1-era references per PATTERNS.md "Fold targets" annotations)
    - C:\Users\pzhly\Documents\GitHub\easyk6\CLAUDE.md (Technology Stack tables lines 22-44, Alternatives Considered lines 50-55, What NOT to Use lines 57-61 — source for §1 lifted-patterns + §2 simplifications)
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\PROJECT.md (Out-of-Scope list at lines 32-37, Key Decisions table at lines 55-62 — source for §2 deferrals and §5 decision-log pointers)
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\STATE.md (Decisions block at lines 64-100 — source for §5 chronological index, init through Phase 4)
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\phases\03-smoke-scenarios-supported-execution\03-02-SUMMARY.md (lines 340-360 — k6 1.5 runtime gotchas + carry-forward bullet shape for §4 caveats and §2 tonal model)
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\phases\04-example-profiles-output-surface\04-RESEARCH.md (lines 420-445 — Pitfall 1 `browser_http_req_duration` evidence for §4 caveats)
    - C:\Users\pzhly\Documents\GitHub\easyk6\package.json (lines 6-16 — verify npm-script names referenced in §1/§3 match the manifest verbatim)
  </read_first>
  <action>
Create ARCHITECTURE.md at the repo root (flat layout per D-04 — NOT under docs/). The file MUST contain exactly these five H2 sections in this exact order, matching D-07.1 through D-07.5:

1. `## Adapted from \`ir-perf-k6\``
2. `## Simplified on purpose`
3. `## Upstream reuse pipeline`
4. `## k6 1.5 runtime caveats`
5. `## Decision log`

Open the file with an H1 title (`# EasyK6 Architecture`) followed by one short framing paragraph (2-3 sentences max) that states what ARCHITECTURE.md is (the design-narrative companion to README) and who it is for (a technical reviewer who wants to know what was adapted, what was simplified, and why). Tone: first-person engineering narrative is recommended (matches Phase SUMMARY.md voice per PATTERNS.md §A) but third-person reference is acceptable. NO Table of Contents — the 5 H2 sections render naturally in GitHub's sidebar.

Section §1 "Adapted from `ir-perf-k6`" — for each of the four lifted patterns below, write one paragraph or one bullet that names the pattern, names the phase that landed it, and (per PATTERNS.md §B "Recommended closing pattern") ends with a one-line "what got dropped" callout that pre-references §2. Use these four patterns verbatim (drawn from D-07.1):
- Scenario registry (Phase 3 — `lib/scenarios/index.ts` + kebab-case IDs; what got dropped: multi-team scenario matrix)
- TypeScript / Vite build pipeline (Phase 1 — `vite.config.ts` + `tsconfig.json`; what got dropped: Docker browser image as default execution path)
- Profile-keyed runner (Phase 3 — `resolveEntryFile(profile) → dist/simulations/${profile}.js`; what got dropped: per-scenario entry files)
- `handleSummary` factory (Phase 4 — `lib/simulations/lib/summary.ts::makeHandleSummary(...)`; what got dropped: HTML dashboard / k6 Cloud)

Section §2 "Simplified on purpose" — author exactly six bullets in the "we did NOT include X because Y" shape per PATTERNS.md §C. Use these six items verbatim in this order (sourced from CLAUDE.md "What NOT to Use" + PROJECT.md "Out of Scope" + REQUIREMENTS.md v2 list):
- **No Grafana / OTEL integration** — `OBS-01` / `OBS-02` are v2 work. v1 emits `reports/<profile>-<scenario>.md` so a reviewer reads results in 30 seconds without standing up a dashboard.
- **No Kubernetes execution** — local-first showcase scope. `ir-perf-k6` runs in EKS via k6-operator; EasyK6 v1 runs on a laptop and that is the point.
- **No multi-upstream adapter** — `easyPlaywright` is the permanent v1 model. `FRAME-03` opens that surface later.
- **No enterprise scenario matrix** — two scenarios (`home-smoke`, `blog-post-smoke`) prove the registry; more breadth without more depth would dilute the signal.
- **No cloud k6** — local-first holds.
- **No CI/CD smoke-on-PR** — `FRAME-02` is v2 work.

Append at the end of §2 a `### Known limitations & deferred work` subsection (per D-09 option (b) — planner chose dual-surface (c)). List the four carry-forward items in the tonal model from `.planning/phases/03-smoke-scenarios-supported-execution/03-02-SUMMARY.md:347-358` (sober, pointer-driven, no marketing). Use these exact bullets:
- **BUILD-02** (runtime-config fail-fast on missing/invalid env) — `.planning/REQUIREMENTS.md:18` still shows `[ ]`. Phase 1 closed the foundation; the strict-validation closure was descoped pre-Phase-2 and remains open. See `.planning/STATE.md` for current position.
- **SCEN-02** (smoke real-journey vs demo target) — `.planning/REQUIREMENTS.md:23` still shows `[ ]` despite Phase 03-02 capturing real-run evidence against `https://othrondir.github.io/QAbbalah/` (exit 0, 3/3 D-66 thresholds pass). Checkbox-hygiene gap, not missing work. See `.planning/phases/03-smoke-scenarios-supported-execution/03-02-SUMMARY.md`.
- **F-01** (capacity real-run evidence) — deferred from Plan 04-02 because the recruiter laptop saturated after the load real-run. Static gates green: 7 unit tests, build emits `dist/simulations/capacity.js`, `validate:build` includes the entry, dry-run prints the spawn argv. Closure path: `npm run example:capacity` against the demo target on a quieter host.
- **F-02** (smoke single-iteration LCP `n/a`) — when only one iteration sample fires, the Key Metrics row for `browser_web_vital_lcp` renders the literal string `n/a (no samples — browser scenario)`. Informational, non-blocking. See `.planning/phases/04-example-profiles-output-surface/04-CONTEXT.md` F-02.

Section §3 "Upstream reuse pipeline" — narrate the pipeline as numbered steps (`easyPlaywright` → `src/pages/` via `npm run sync:src` → `lib/pages/` via `npm run convert-pages` → `lib/pages-k6-patches/` persistent overrides). Then fold the PROJECT_STRUCTURE.md content per D-05 + PATTERNS.md "Fold targets (valuable)":

a. **Primary Layout tree** — author a fresh fenced ```text code block tree based on the current repo state captured in the `<interfaces>` block above. Do NOT copy PROJECT_STRUCTURE.md:7-38 verbatim — that tree still shows `k6/simulations/smoke/smoke-shell.test.ts` which Phase 03-02 deleted. The new tree MUST show: `lib/` (config, pages, pages-k6-patches, scenarios, simulations [with `lib/`, `capacity.ts`, `load.ts`, `smoke.ts`], vendor), `scripts/` (convert-pages.mjs, lib/, perf-runner.mjs, sync-src.mjs, validate-build.mjs), `src/pages/` (AboutPage.ts, BasePage.ts, HomePage.ts, PostPage.ts, components/, index.ts, .sync-meta.json note), `legacy-js/` (archive), `tests/` (unit + integration), top-level files (CLAUDE.md, README.md, ARCHITECTURE.md, package.json, tsconfig.json, vite.config.ts, package-lock.json). Mention `dist/` and `node_modules/` and `reports/` parenthetically as gitignored runtime artifacts — do not list them in the tree itself.

b. **Boundary Definitions** — fold lines 42-49 of PROJECT_STRUCTURE.md, STRIPPING all `(Phase 2 next plan)` / `(Phase 2 next plan; survives the convert wipe)` / `(Phase 2 next plan; injected at convert time)` parentheticals (per PATTERNS.md "Refresh required"). Update `k6/simulations = executable k6 entrypoints that build into dist/tests/...` to read `lib/simulations = executable k6 entrypoints that build into dist/simulations/<profile>.js` (per PATTERNS.md "Refresh required" item 2 — Phase 3 moved canonical entries). Keep the four primary boundary labels in this exact order: `src/pages`, `lib/pages`, `lib/pages-k6-patches`, `legacy-js`. Add `src/pages/.sync-meta.json` as the fifth provenance label.

c. **Root Directories narrative** — fold the WHY sentences from PROJECT_STRUCTURE.md:52-90 (why `lib/` exists, why `scripts/` exists, why `src/` is rewritten by sync, why `legacy-js/` is archived). Refresh any Phase-2-future reference. Drop the legacy-js inner-tree detail (`legacy-js/config/config.js` line 87 etc.) — recruiter does not need that depth; one sentence ("preserves the original JavaScript starter for before/after comparison") suffices.

d. **Sync Provenance table** — fold the 5-row table from PROJECT_STRUCTURE.md:102-114 verbatim (`source`, `mode`, `branch`, `commit`, `syncedAt` rows). This is a strong recruiter-readable signal per PATTERNS.md "Fold targets (valuable)".

e. **legacy-js/ boundary callout (D-08 mandatory)** — include exactly this sentence as a standalone paragraph at the end of §3 (or appended to the Boundary Definitions list): `\`legacy-js/\` preserves the original JavaScript starter for before/after comparison — it is NOT the active codebase.`

DROP these PROJECT_STRUCTURE.md sections per PATTERNS.md "Fold targets (drop — stale)":
- lines 92-96 Build Flow section (covered accurately by README §Commands)
- lines 98-100 Legacy Policy section (D-08 callout in §3e replaces it)

Section §4 "k6 1.5 runtime caveats" — author exactly four bullets per D-07.4 + PATTERNS.md §E, in this order. Each bullet names the caveat, names the mitigation file/symbol, and ends with a citation pointer. CITE evidence (do not re-prove):
- **No global `URL` constructor** — k6 1.5's goja runtime does not ship `new URL(...)`. Mitigation: `lib/config/runtime-config.ts::normalizeBaseUrl` uses a regex matcher (`^(https?://[^/\s]+)(/.*)?$`) plus bare-host trailing-slash normalization.
- **System env vars don't reach `__ENV`** — k6 1.5 does NOT inherit shell env vars into `__ENV` without `--include-system-env-vars`. Mitigation: `scripts/perf-runner.mjs::buildK6Args` emits `BASE_URL`, `K6_PROFILE`, `K6_SCENARIO`, `K6_DEMO`, and `SCENARIO` as explicit `-e KEY=VALUE` flags on every spawn.
- **`exec.test.abort` for fail-fast (not raw `throw`)** — k6 1.5 reports exit 0 when all thresholds pass even if the iteration body throws. Mitigation: `lib/simulations/smoke.ts` calls `exec.test.abort(message)` from `k6/execution` before the defensive throw, triggering k6 exit code 108 → runner re-rejects → npm exit 1.
- **Browser scenarios route HTTP through chromium** — `http_req_*` metrics read 0 samples for `k6/browser` scenarios. Mitigation: threshold strings target `browser_http_req_duration` (load + capacity profiles per Phase 4 D-03 / D-08 amendment). Evidence: `.planning/phases/04-example-profiles-output-surface/04-RESEARCH.md` §5 Pitfall 1 + Phase 03-02 SUMMARY Run 1 (`http_req_failed: 0.00% 0 out of 0`).

Close §4 with a single citation footer line: `Detailed evidence in \`.planning/phases/03-smoke-scenarios-supported-execution/03-02-SUMMARY.md\` (Plan 03-02 deviation commit \`7d629ba\`) and \`.planning/phases/04-example-profiles-output-surface/04-RESEARCH.md\` §5 Pitfall 1.`

Section §5 "Decision log" — author a one-line-per-decision INDEX (not a re-statement) per D-07.5 + PATTERNS.md §F. Each bullet uses the shape `**[Phase N]** — [one-line decision]. See \`<pointer>\`.`. Include exactly these six entries in chronological order (curated from STATE.md Decisions lines 64-100 and PROJECT.md Key Decisions lines 55-62):
- **[Init]** — `easyPlaywright` is the permanent upstream POM model; smoke is the supported v1 workflow; Grafana / OTEL is deferred to v2. See `.planning/PROJECT.md` Key Decisions.
- **[Phase 1]** — archived the original JavaScript starter under `legacy-js/` and adopted TypeScript-first new architecture. See `.planning/phases/01-foundation-project-shape/01-CONTEXT.md`.
- **[Phase 2]** — built the Node-ESM converter (`scripts/convert-pages.mjs`) and the `lib/pages-k6-patches/` injection mechanism that survives sync→convert→re-sync cycles. See `.planning/phases/02-upstream-sync-k6-adaptation/02-03-SUMMARY.md`.
- **[Phase 3]** — adopted a central scenario registry (`lib/scenarios/index.ts`) with kebab-case IDs and profile-keyed `resolveEntryFile`. See `.planning/phases/03-smoke-scenarios-supported-execution/03-CONTEXT.md`.
- **[Phase 3 deviation]** — codified four k6 1.5 runtime caveats in code (commit `7d629ba`): no global URL, env-flag plumbing, `exec.test.abort` for fail-fast, PostPage selector fallback. See `.planning/phases/03-smoke-scenarios-supported-execution/03-02-SUMMARY.md`.
- **[Phase 4]** — introduced `makeHandleSummary` factory at `lib/simulations/lib/summary.ts` and retargeted threshold strings to `browser_http_req_duration` for load + capacity. See `.planning/phases/04-example-profiles-output-surface/04-CONTEXT.md` D-03 / D-16.

Close §5 with one pointer sentence: `Full chronological decision history lives in \`.planning/STATE.md\` (Decisions block) and `.planning/phases/<N>-*/<N>-CONTEXT.md` for per-phase locked decisions.`

Do NOT embed inline screenshots / GIFs (out-of-scope per CONTEXT). Do NOT create a `docs/` subdirectory (D-04). Do NOT cross-link to QUICKSTART.md or CONTRIBUTING.md (both deleted in Task 3). Do NOT cross-link to PROJECT_STRUCTURE.md as a separate file (deleted in Task 3 after this fold).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ok=$true; if (-not (Test-Path 'ARCHITECTURE.md')) { Write-Host 'FAIL: ARCHITECTURE.md missing'; $ok=$false }; $c=Get-Content 'ARCHITECTURE.md' -Raw; foreach ($h in @('## Adapted from `ir-perf-k6`','## Simplified on purpose','## Upstream reuse pipeline','## k6 1.5 runtime caveats','## Decision log')) { if ($c -notmatch [regex]::Escape($h)) { Write-Host \"FAIL: missing section $h\"; $ok=$false } }; foreach ($p in @('src/pages','lib/pages','lib/pages-k6-patches','legacy-js','normalizeBaseUrl','browser_http_req_duration','exec.test.abort','BUILD-02','SCEN-02','F-01','F-02','03-02-SUMMARY.md','04-RESEARCH.md','.planning/STATE.md')) { if ($c -notmatch [regex]::Escape($p)) { Write-Host \"FAIL: missing pattern $p\"; $ok=$false } }; $h2 = ([regex]::Matches($c,'(?m)^## ')).Count; if ($h2 -ne 5) { Write-Host \"FAIL: expected 5 H2 sections, found $h2\"; $ok=$false }; if ($ok) { Write-Host 'PASS' } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `ARCHITECTURE.md` exists at repo root (`test -f ARCHITECTURE.md`)
    - File contains exactly 5 H2 headings (`(Get-Content ARCHITECTURE.md | Select-String '^## ').Count -eq 5`)
    - The 5 H2 headings appear in D-07 order (`## Adapted from \`ir-perf-k6\`` then `## Simplified on purpose` then `## Upstream reuse pipeline` then `## k6 1.5 runtime caveats` then `## Decision log`)
    - §3 contains all four boundary labels `src/pages`, `lib/pages`, `lib/pages-k6-patches`, `legacy-js` (grep returns 1+ match each)
    - §3 contains the legacy-js D-08 callout sentence containing the literal substring `NOT the active codebase`
    - §4 references each of: `normalizeBaseUrl`, `browser_http_req_duration`, `exec.test.abort`, `--include-system-env-vars`
    - §4 cites both `03-02-SUMMARY.md` AND `04-RESEARCH.md` (or `7d629ba`)
    - §2 contains the four carry-forward IDs `BUILD-02`, `SCEN-02`, `F-01`, `F-02` (subsection `### Known limitations & deferred work`)
    - §5 contains at least 6 bullets starting with `**[` and references `.planning/STATE.md`
    - File contains NO references to `QUICKSTART.md`, `CONTRIBUTING.md`, or `PROJECT_STRUCTURE.md` as separate-doc links (grep `\[.*\]\((QUICKSTART|CONTRIBUTING|PROJECT_STRUCTURE)\.md\)` returns zero matches)
    - File contains NO `Phase 2 next plan` parentheticals (grep returns zero matches)
    - No `docs/` subdirectory created (`test ! -d docs`)
  </acceptance_criteria>
  <done>ARCHITECTURE.md exists at repo root with 5 H2 sections per D-07, PROJECT_STRUCTURE.md content folded into §3 (refreshed layout tree, boundary definitions, sync provenance table, legacy-js D-08 callout), §4 cites Phase 03-02 + Phase 04 evidence without re-proving, §5 indexes `.planning/` as a pointer list, BUILD-02 / SCEN-02 / F-01 / F-02 surfaced in §2 `### Known limitations & deferred work` subsection.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md top-to-bottom per D-06, preserving the Phase 4 Quickstart table verbatim</name>
  <files>README.md</files>
  <read_first>
    - C:\Users\pzhly\Documents\GitHub\easyk6\README.md (current full file — source of the Phase 4 Quickstart block at lines 7-17 that MUST be preserved byte-identical, and source of Patterns D/E/F/G that get refreshed shape per PATTERNS.md)
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\phases\05-showcase-docs-recruiter-polish\05-CONTEXT.md (D-06 full rewrite contract; D-09 carry-forward option (a) "honest mention" placement; Claude's Discretion: optional ASCII tree, optional badges, optional `reports/` excerpt, optional cross-link footer)
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\phases\05-showcase-docs-recruiter-polish\05-PATTERNS.md (README Pattern Assignments §A-§I — section-by-section guidance with verbatim preservation block, refreshed Upstream Reuse 3-step shape, refreshed Commands block, Legacy Note + Next Reference rewrites; §Verbatim-preservation discipline shared pattern)
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\phases\04-example-profiles-output-surface\04-CONTEXT.md (D-13 + D-15 locked Quickstart table contract that Phase 5 must NOT relitigate)
    - C:\Users\pzhly\Documents\GitHub\easyk6\package.json (lines 6-16 — npm-script names to quote verbatim in §Commands; D-06 rewrite must not invent script names)
    - C:\Users\pzhly\Documents\GitHub\easyk6\ARCHITECTURE.md (produced by Task 1; verify the linked title and §3 fold landed before authoring the §Architecture pointer sentence)
  </read_first>
  <action>
Replace the entire current `README.md` (lines 1-110) with a fresh recruiter-first rewrite. The file structure MUST be these sections in this order:

1. `# EasyK6` (H1)
2. Lede paragraph (2-3 short sentences — see Pattern A guidance)
3. `## Quickstart` — THE VERBATIM PRESERVATION BLOCK (see Pattern B)
4. `## Upstream Reuse` (refreshed 3-step pipeline narrative)
5. `## Commands` (recruiter-scannable bash block)
6. `## Runtime Config` (preserve the existing block, verify precedence line)
7. `## Architecture` (single-paragraph pointer to ARCHITECTURE.md)
8. `## Legacy Note` (D-08 — short paragraph; `legacy-js/` boundary)
9. `## Known carry-forward` (D-09 option (a) — sober pointer list; planner chose dual-surface (c), this is the README half)

**Lede paragraph** — open with the existing accurate framing line from current README:5 (`EasyK6 is a recruiter-facing k6 browser performance framework that reuses Playwright page objects as the long-term upstream model while keeping the local developer experience simple.`). Append one second sentence that names the upstream-reuse value proposition concisely. Example shape (planner may adjust wording but MUST keep the recruiter-readable + upstream-reuse signal): `One Playwright POM source — \`easyPlaywright\` — powers maintainable k6 browser smoke tests through a clean architecture you can read, trust, and run locally.` REMOVE the current line 5 (`Phase 1 establishes the build foundation, repo boundaries, and the shared runtime-config contract behind \`npm run smoke\` and \`npm run perf\`.`) — it is stale by four phases per D-06 + PATTERNS.md Pattern A "DELETE" note.

**§Quickstart (VERBATIM PRESERVATION — D-06 + Phase 4 D-13/D-15 contract)** — paste the 11-line block EXACTLY as it appears in the `<interfaces>` Phase 4 Quickstart block above (which is byte-identical to current README.md:7-17). The block opens with the `## Quickstart` heading line, a blank line, the prose sentence `Smoke is the supported demo path; load and capacity are illustrative examples sharing the same architecture.`, a blank line, the 3-column table with header row `| Command | Status | What it does |` and separator `|---|---|---|`, three data rows (`npm run smoke` / `**Supported**`, `npm run example:load` / `_Example_`, `npm run example:capacity` / `_Example_`) with the exact parenthetical descriptions ("ramping-vus, 5 VUs, ~2 min" and "ramping-arrival-rate, find-the-ceiling, ~3 min"), a blank line, and the trailing sentence `All three profiles write recruiter-readable artifacts to \`reports/<profile>-<scenario>.md\` + \`reports/<profile>-<scenario>.json\` (gitignored).`. The bold `**Supported**`, italic `_Example_`, and exact strings are NON-NEGOTIABLE per the Phase 4 D-15 contract.

**§Upstream Reuse** — REFRESH the current 3-step numbered list per PATTERNS.md Pattern E. Strip the Phase-2-future parentheticals (`(lands in the next Phase 2 plan)` — both occurrences). Use this shape verbatim:

```markdown
## Upstream Reuse

The repo treats `easyPlaywright` as the permanent upstream Page Object source. The flow is linear:

1. `npm run sync:src` — copy `easyPlaywright/src/pages/` into `easyk6/src/pages/` (idempotent; wipes the local folder before copy).
2. `npm run convert-pages` — produce k6-safe modules under `lib/pages/`.
3. `lib/pages-k6-patches/` — k6-only methods that survive every re-sync / re-convert cycle.
```

Keep the second paragraph from current README:73 about source override options: `Run sync against a sibling clone (default), an arbitrary path (\`--source ./fork\`), or a remote (\`--repo https://github.com/Othrondir/easyPlaywright.git --branch main\`). Pass \`--yes\` or set \`CI=1\` to skip the confirmation prompt before the wipe.`

**§Commands** — keep the fenced bash block from current README:55-63 verbatim (all 7 commands: `npm install`, `npm run build`, `npm run validate:build`, `npm run smoke`, `npm run perf`, `npm run sync:src`, `npm run convert-pages`). DELETE the entire "Current command status" bullet list at current README:75-82 per PATTERNS.md Pattern D — its `npm run convert-pages stays reserved for the next Phase 2 plan` line is stale by three phases. Replace it with a single-paragraph descriptor under the bash block:

```markdown
- `npm run smoke` is the supported recruiter demo path; it runs the smoke profile against the demo target with explicit `--demo`.
- `npm run example:load` and `npm run example:capacity` run the illustrative profiles (see Quickstart).
- `npm run perf` exposes the shared runtime-config CLI grammar (`--profile`, `--scenario`, `--base-url`, `--demo`, `--dry-run`) for real-target overrides.
- `npm run sync:src` mirrors upstream `easyPlaywright/src/pages/` into local `src/pages/` and writes `.sync-meta.json`.
- `npm run convert-pages` converts synced POMs in `src/pages/` into k6-safe modules under `lib/pages/`, preserving `lib/pages/base/` and concatenating any matching `lib/pages-k6-patches/<rel>.k6-patch.ts` fragment.
- `npm run build` and `npm run validate:build` cover the Vite bundle and the required-files emit check.
```

**§Runtime Config** — KEEP the existing block from current README:84-102 unchanged in shape. The `.env` example, the precedence sentence (`Runtime precedence is \`CLI > .env > shell env > built-in demo defaults\`.`), the example flows, and the shell-env note all stay. PATTERNS.md Pattern F flags this as Phase-1-stable. If a stale phase reference surfaces during the rewrite, refresh it; otherwise leave the prose untouched.

**§Architecture** — REPLACE the current `## Architecture First` heading + ASCII tree block + boundary-labels list (current README:19-51) with a single-paragraph pointer per PATTERNS.md Pattern C:

```markdown
## Architecture

For the design narrative — what was adapted from `ir-perf-k6`, what was simplified, and why — see [ARCHITECTURE.md](ARCHITECTURE.md).
```

The ASCII tree and boundary labels now live in ARCHITECTURE.md §3 (Task 1). Keeping them duplicated in README adds visual noise without adding signal. The single-pointer paragraph is the cleanest cross-link given the flat layout (D-04).

**§Legacy Note** — KEEP the current short paragraph from README:104-106 verbatim:

```markdown
## Legacy Note

The original JavaScript starter remains available under `legacy-js/` for reference and comparison. It is no longer the primary architecture story for this repository.
```

**§Known carry-forward** (NEW — D-09 option (a), planner chose dual-surface (c)) — add a short section at the end of the file that names the four carry-forward items in the tonal model from `.planning/phases/03-smoke-scenarios-supported-execution/03-02-SUMMARY.md:347-358`:

```markdown
## Known carry-forward

A small set of items intentionally remains open for transparency over implied completeness:

- **BUILD-02** — runtime-config fail-fast on missing/invalid env is partially implemented (foundation in Phase 1; strict validation remains open). See `ARCHITECTURE.md` §Simplified on purpose / Known limitations.
- **SCEN-02** — smoke real-journey vs demo-target captured real-run evidence in Phase 3 (exit 0, three thresholds pass against `https://othrondir.github.io/QAbbalah/`); the `REQUIREMENTS.md` checkbox is a hygiene gap, not missing work.
- **F-01** — capacity real-run evidence was deferred from Plan 04-02 due to host saturation after the load run. All static gates (unit tests, build, validate, dry-run) are green; closure is one `npm run example:capacity` on a quieter host.
- **F-02** — single-iteration smoke renders `n/a (no samples — browser scenario)` for the LCP Key Metrics row. Informational, non-blocking.
```

**DELETE the current `## Next Reference` section** (README:108-110 — the line `See [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) for the folder-by-folder breakdown of the new layout.`). It points at a file Phase 5 deletes in Task 3, and the §Architecture pointer above already covers the drill-down path.

**Claude's Discretion items (planner picks each — these are explicit non-contracts per CONTEXT):**
- Inline `reports/smoke-home-smoke.md` excerpt: DO NOT include (adds visual noise without signal; recruiter can run `npm run smoke` to see the real artifact).
- Small ASCII tree in README §Architecture: DO NOT include (the single-paragraph pointer reads cleaner; ARCHITECTURE.md §3 owns the tree).
- Badges (Node / k6 version / license): DO NOT include in this pass (no objective signal vs visual noise tradeoff favors omitting; future polish pass can add if recruiter feedback says otherwise).
- `docs/` cross-link block at bottom: DO NOT include (flat layout per D-04; no `docs/` subdir exists).

The rewritten README is bounded by recruiter-first framing + smoke-first scope + locked Quickstart-table preservation. Nothing else is added.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ok=$true; if (-not (Test-Path 'README.md')) { Write-Host 'FAIL: README.md missing'; $ok=$false }; $c=Get-Content 'README.md' -Raw; foreach ($h in @('## Quickstart','## Upstream Reuse','## Commands','## Runtime Config','## Architecture','## Legacy Note','## Known carry-forward')) { if ($c -notmatch [regex]::Escape($h)) { Write-Host \"FAIL: missing section $h\"; $ok=$false } }; foreach ($p in @('npm run smoke','npm run example:load','npm run example:capacity','**Supported**','_Example_','ramping-vus, 5 VUs, ~2 min','ramping-arrival-rate, find-the-ceiling, ~3 min','reports/<profile>-<scenario>.md','easyPlaywright','ARCHITECTURE.md','legacy-js/','BUILD-02','SCEN-02','F-01','F-02')) { if ($c -notmatch [regex]::Escape($p)) { Write-Host \"FAIL: missing pattern $p\"; $ok=$false } }; foreach ($stale in @('Phase 1 establishes','Phase 2 next plan','lands in the next Phase 2 plan','PROJECT_STRUCTURE.md','QUICKSTART.md','CONTRIBUTING.md','## Next Reference')) { if ($c -match [regex]::Escape($stale)) { Write-Host \"FAIL: stale or deleted-file reference present: $stale\"; $ok=$false } }; if ($ok) { Write-Host 'PASS' } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists and is rewritten (file modified time newer than pre-Phase-5)
    - `## Quickstart` heading appears at H2 level
    - All 11 verbatim Phase 4 Quickstart lines are present byte-identical (bold `**Supported**`, italic `_Example_`, exact script names `npm run smoke` / `npm run example:load` / `npm run example:capacity`, exact parenthetical descriptions `ramping-vus, 5 VUs, ~2 min` and `ramping-arrival-rate, find-the-ceiling, ~3 min`, exact trailing sentence about `reports/<profile>-<scenario>.md`)
    - `## Architecture` section contains a link to `ARCHITECTURE.md` (markdown link form `[ARCHITECTURE.md](ARCHITECTURE.md)`)
    - `## Known carry-forward` section contains all four IDs: `BUILD-02`, `SCEN-02`, `F-01`, `F-02`
    - File contains ZERO occurrences of `QUICKSTART.md`, `CONTRIBUTING.md`, or `PROJECT_STRUCTURE.md` (deleted-file dangling-link sweep — grep returns zero matches)
    - File contains ZERO occurrences of stale framing: `Phase 1 establishes`, `Phase 2 next plan`, `lands in the next Phase 2 plan`, `## Next Reference`
    - `## Legacy Note` section contains the literal substring `legacy-js/` and `no longer the primary architecture story`
    - `## Runtime Config` contains the precedence line `CLI > .env > shell env > built-in demo defaults`
    - File contains all seven npm commands from the bash block: `npm install`, `npm run build`, `npm run validate:build`, `npm run smoke`, `npm run perf`, `npm run sync:src`, `npm run convert-pages`
  </acceptance_criteria>
  <done>README.md is rewritten top-to-bottom per D-06; the Phase 4 Quickstart Supported-vs-Example table is preserved byte-identical; ARCHITECTURE.md is linked via single-paragraph pointer under §Architecture; BUILD-02 / SCEN-02 / F-01 / F-02 are surfaced under §Known carry-forward; zero references to deleted files (QUICKSTART.md, CONTRIBUTING.md, PROJECT_STRUCTURE.md) remain; zero stale Phase-1 / Phase-2-future framing remains.</done>
</task>

<task type="auto">
  <name>Task 3: Delete QUICKSTART.md, CONTRIBUTING.md, PROJECT_STRUCTURE.md and run the dangling-reference sweep</name>
  <files>QUICKSTART.md, CONTRIBUTING.md, PROJECT_STRUCTURE.md</files>
  <read_first>
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\phases\05-showcase-docs-recruiter-polish\05-CONTEXT.md (D-02 QUICKSTART.md delete rationale, D-03 CONTRIBUTING.md delete rationale, D-05 PROJECT_STRUCTURE.md fold-then-delete sequencing)
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\phases\05-showcase-docs-recruiter-polish\05-PATTERNS.md (Cross-reference cleanup notes under each deletion target — `README.md` line 110 and line 43 references to `PROJECT_STRUCTURE.md` that Task 2 already handled; CLAUDE.md sweep notes)
    - C:\Users\pzhly\Documents\GitHub\easyk6\ARCHITECTURE.md (produced by Task 1 — verify the fold-from-PROJECT_STRUCTURE.md happened before this task deletes the source file)
    - C:\Users\pzhly\Documents\GitHub\easyk6\README.md (produced by Task 2 — verify the README rewrite stripped the `PROJECT_STRUCTURE.md` line at old line 110 and old line 43 before deletion)
    - C:\Users\pzhly\Documents\GitHub\easyk6\CLAUDE.md (full file — verify no in-body references to the three deletion targets remain after Task 2; CLAUDE.md was not in scope of Task 2 but should be audited here)
  </read_first>
  <action>
This task runs AFTER Task 1 (which folds PROJECT_STRUCTURE.md content into ARCHITECTURE.md §3) and Task 2 (which strips README links to the three deletion targets). Verify both predecessors landed before deleting.

**Pre-deletion gate (mandatory — execute first):**

Run two grep audits in PowerShell. Both MUST return zero matches before any deletion happens; if either returns matches, STOP and surface back to the planner — Tasks 1 or 2 failed to scrub a reference.

```powershell
# Gate 1: README.md must have no remaining references to the three deletion targets
$readme = Get-Content README.md -Raw
foreach ($f in @('QUICKSTART.md','CONTRIBUTING.md','PROJECT_STRUCTURE.md')) {
  if ($readme -match [regex]::Escape($f)) { throw "GATE FAIL: README.md still references $f" }
}

# Gate 2: ARCHITECTURE.md must have folded PROJECT_STRUCTURE.md content (sanity — already verified by Task 1 acceptance)
$arch = Get-Content ARCHITECTURE.md -Raw
foreach ($label in @('src/pages','lib/pages','lib/pages-k6-patches','legacy-js')) {
  if ($arch -notmatch [regex]::Escape($label)) { throw "GATE FAIL: ARCHITECTURE.md missing folded boundary label $label" }
}
```

**Deletions (execute in order):**

1. Delete `QUICKSTART.md` from the repo root. PowerShell: `Remove-Item -Path 'QUICKSTART.md' -Force`. Verify with `Test-Path QUICKSTART.md` returning `False`.
2. Delete `CONTRIBUTING.md` from the repo root. PowerShell: `Remove-Item -Path 'CONTRIBUTING.md' -Force`. Verify with `Test-Path CONTRIBUTING.md` returning `False`.
3. Delete `PROJECT_STRUCTURE.md` from the repo root. PowerShell: `Remove-Item -Path 'PROJECT_STRUCTURE.md' -Force`. Verify with `Test-Path PROJECT_STRUCTURE.md` returning `False`.

**Post-deletion dangling-reference sweep (mandatory):**

Run a recursive grep across the top-level repo-root docs to confirm no in-body reference to the three deleted files survives. Scope: `README.md`, `ARCHITECTURE.md`, `CLAUDE.md`, `package.json`. Do NOT sweep `.planning/` — planning history is allowed to reference deleted files (per PATTERNS.md "Cross-reference cleanup: `.planning/` references are historical — leave as-is").

```powershell
$targets = @('QUICKSTART.md','CONTRIBUTING.md','PROJECT_STRUCTURE.md')
$files = @('README.md','ARCHITECTURE.md','CLAUDE.md','package.json')
$failed = $false
foreach ($file in $files) {
  if (-not (Test-Path $file)) { continue }
  $body = Get-Content $file -Raw
  foreach ($target in $targets) {
    if ($body -match [regex]::Escape($target)) {
      Write-Host "DANGLING: $file still references $target"
      $failed = $true
    }
  }
}
if ($failed) { throw 'Dangling-reference sweep failed' }
```

If a CLAUDE.md reference surfaces during the sweep (none expected per PATTERNS.md), surface to the planner — CLAUDE.md edits are NOT in the locked Task 4 polish scope unless they are a sweep necessity. If absolutely required for sweep cleanliness, remove the offending reference with a minimal targeted Edit, NOT a full CLAUDE.md rewrite.

Do NOT delete any file under `legacy-js/`, `lib/`, `scripts/`, `src/`, `tests/`, `.planning/`, or any other directory. The three deletions are scoped to the repo root.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ok=$true; foreach ($f in @('QUICKSTART.md','CONTRIBUTING.md','PROJECT_STRUCTURE.md')) { if (Test-Path $f) { Write-Host \"FAIL: $f still exists\"; $ok=$false } }; foreach ($file in @('README.md','ARCHITECTURE.md','CLAUDE.md','package.json')) { if (-not (Test-Path $file)) { continue }; $body=Get-Content $file -Raw; foreach ($target in @('QUICKSTART.md','CONTRIBUTING.md','PROJECT_STRUCTURE.md')) { if ($body -match [regex]::Escape($target)) { Write-Host \"FAIL: $file still references $target\"; $ok=$false } } }; if ($ok) { Write-Host 'PASS' } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `Test-Path QUICKSTART.md` returns `False`
    - `Test-Path CONTRIBUTING.md` returns `False`
    - `Test-Path PROJECT_STRUCTURE.md` returns `False`
    - Grep across `README.md`, `ARCHITECTURE.md`, `CLAUDE.md`, `package.json` returns ZERO matches for `QUICKSTART.md`
    - Grep across the same four files returns ZERO matches for `CONTRIBUTING.md`
    - Grep across the same four files returns ZERO matches for `PROJECT_STRUCTURE.md`
    - No files outside the repo root were modified (`legacy-js/`, `lib/`, `scripts/`, `src/`, `tests/`, `.planning/` left untouched — git status diff scope shows only the three deletions plus optional CLAUDE.md sweep fix if any)
    - `git status` shows exactly three deletions (`D QUICKSTART.md`, `D CONTRIBUTING.md`, `D PROJECT_STRUCTURE.md`) plus modifications from Tasks 1, 2, and 4
  </acceptance_criteria>
  <done>Three stale docs deleted from working tree; no dangling references in any recruiter-visible doc; the GitHub file list now shows the two-file canonical doc set (README.md + ARCHITECTURE.md) per D-01.</done>
</task>

<task type="auto">
  <name>Task 4: Optional polish — package.json description + CLAUDE.md placeholder refresh (Claude's Discretion per CONTEXT)</name>
  <files>package.json, CLAUDE.md</files>
  <read_first>
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\phases\05-showcase-docs-recruiter-polish\05-CONTEXT.md (Claude's Discretion section — `package.json` description refresh + CLAUDE.md Conventions/Architecture refresh are both explicit non-contracts; planner decides yes/no/partial)
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\phases\05-showcase-docs-recruiter-polish\05-PATTERNS.md (`package.json` description Pattern note — refresh shape; CLAUDE.md placeholders Pattern note — refresh shape)
    - C:\Users\pzhly\Documents\GitHub\easyk6\package.json (current `description` field at line 4 — `"K6 Performance Testing Framework with POM (Page Object Model) architecture"`)
    - C:\Users\pzhly\Documents\GitHub\easyk6\CLAUDE.md (current Conventions placeholder at lines 83-85 within `<!-- GSD:conventions-start -->`...`<!-- GSD:conventions-end -->`; current Architecture placeholder at lines 88-91 within `<!-- GSD:architecture-start -->`...`<!-- GSD:architecture-end -->`)
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\PROJECT.md (Core Value line at line 16 — recruiter-tuned description source material)
    - C:\Users\pzhly\Documents\GitHub\easyk6\ARCHITECTURE.md (produced by Task 1 — referenced by the refreshed CLAUDE.md Architecture section as the canonical pointer)
  </read_first>
  <action>
**Planner decision: EXECUTE both refreshes** (the package.json description is a one-line polish that aligns the manifest with the recruiter-first framing; the CLAUDE.md placeholders read as unfinished and a recruiter scanning the repo's GitHub file list will see them). Both are minimal, additive changes — no contract risk.

**Edit 1: `package.json` description refresh**

Replace line 4 of `package.json`:

Current value (current line 4):
```json
  "description": "K6 Performance Testing Framework with POM (Page Object Model) architecture",
```

New value (planner's recruiter-tuned wording sourced from PROJECT.md Core Value line 16):
```json
  "description": "Recruiter-facing k6 browser performance framework; reuses Playwright Page Objects from easyPlaywright as the permanent upstream model.",
```

This is a single-line modification. Touch NO other `package.json` fields (`name`, `version`, `scripts`, `keywords`, `engines`, `devDependencies`, `dependencies` all stay byte-identical). The trailing comma must remain after the new description string.

**Edit 2: `CLAUDE.md` Conventions placeholder refresh**

Replace the placeholder text inside the `<!-- GSD:conventions-start source:CONVENTIONS.md -->`...`<!-- GSD:conventions-end -->` block (current CLAUDE.md lines 82-86). Keep the surrounding GSD comment markers EXACTLY as-is; only the inner content changes.

Current inner content:
```markdown
## Conventions

Conventions not yet established. Will populate as patterns emerge during development.
```

New inner content (sourced from established Phase 1-4 patterns per PATTERNS.md note):
```markdown
## Conventions

- TypeScript strict mode; ESM `.mjs` for Node scripts (`scripts/`).
- Kebab-case scenario IDs registered in `lib/scenarios/index.ts` (`home-smoke`, `blog-post-smoke`).
- goja-safe execution surface: no `new URL(...)`, no `Buffer`, no `process` reads inside `lib/simulations/**` or `lib/simulations/lib/**` — see `ARCHITECTURE.md` §k6 1.5 runtime caveats.
- Profile-keyed simulation entries at `lib/simulations/<profile>.ts` build into `dist/simulations/<profile>.js`.
- Shared simulation helpers live under `lib/simulations/lib/` (goja-safe; single-source data-URL load pattern for unit tests).
- `lib/pages-k6-patches/<rel>.k6-patch.ts` survives every sync→convert cycle via `// #endregion` patch injection.
- `legacy-js/` is archived reference only; new code never lands there.
```

**Edit 3: `CLAUDE.md` Architecture placeholder refresh**

Replace the placeholder text inside the `<!-- GSD:architecture-start source:ARCHITECTURE.md -->`...`<!-- GSD:architecture-end -->` block (current CLAUDE.md lines 88-92). Keep the surrounding GSD comment markers EXACTLY as-is.

Current inner content:
```markdown
## Architecture

Architecture not yet mapped. Follow existing patterns found in the codebase.
```

New inner content (one-sentence pointer per PATTERNS.md note):
```markdown
## Architecture

See `ARCHITECTURE.md` at the repo root for the design narrative — what was adapted from `ir-perf-k6`, what was simplified, and why. The `## Upstream reuse pipeline` section narrates the `src/pages` → `lib/pages` → `lib/pages-k6-patches` flow.
```

Touch NO other CLAUDE.md content. The Project block, Technology Stack block, Workflow Enforcement block, Developer Profile block, and all `<!-- GSD:*-start/end -->` comment markers stay byte-identical.

**Cross-reference sanity check (post-edit):** verify the refreshed CLAUDE.md `## Architecture` block points to the correct file name `ARCHITECTURE.md` (not `architecture.md` or `ARCHITECTURE.MD`).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ok=$true; $pkg=Get-Content package.json -Raw; if ($pkg -notmatch 'Recruiter-facing k6 browser performance framework') { Write-Host 'FAIL: package.json description not refreshed'; $ok=$false }; if ($pkg -match 'K6 Performance Testing Framework with POM') { Write-Host 'FAIL: package.json still has stale description'; $ok=$false }; try { $j = $pkg | ConvertFrom-Json; if (-not $j.name -or -not $j.scripts.smoke) { Write-Host 'FAIL: package.json invalid JSON or missing fields'; $ok=$false } } catch { Write-Host 'FAIL: package.json no longer parses as JSON'; $ok=$false }; $c=Get-Content CLAUDE.md -Raw; if ($c -match 'Conventions not yet established') { Write-Host 'FAIL: CLAUDE.md Conventions placeholder still present'; $ok=$false }; if ($c -match 'Architecture not yet mapped') { Write-Host 'FAIL: CLAUDE.md Architecture placeholder still present'; $ok=$false }; foreach ($marker in @('<!-- GSD:conventions-start','<!-- GSD:conventions-end','<!-- GSD:architecture-start','<!-- GSD:architecture-end','<!-- GSD:project-start','<!-- GSD:stack-start','<!-- GSD:workflow-start','<!-- GSD:profile-start')) { if ($c -notmatch [regex]::Escape($marker)) { Write-Host \"FAIL: GSD marker missing: $marker\"; $ok=$false } }; if ($c -notmatch 'ARCHITECTURE\.md') { Write-Host 'FAIL: CLAUDE.md missing ARCHITECTURE.md pointer'; $ok=$false }; if ($ok) { Write-Host 'PASS' } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `description` field is the new recruiter-tuned wording containing the substring `Recruiter-facing k6 browser performance framework`
    - `package.json` no longer contains the stale string `K6 Performance Testing Framework with POM`
    - `package.json` still parses as valid JSON (`Test-Json` / `ConvertFrom-Json` succeeds)
    - `package.json` `scripts.smoke`, `scripts['example:load']`, `scripts['example:capacity']`, `scripts.build`, `scripts['validate:build']` are unchanged (byte-identical to pre-Phase-5)
    - `CLAUDE.md` no longer contains the substring `Conventions not yet established`
    - `CLAUDE.md` no longer contains the substring `Architecture not yet mapped`
    - `CLAUDE.md` `<!-- GSD:conventions-start ... -->` and `<!-- GSD:conventions-end -->` markers still present
    - `CLAUDE.md` `<!-- GSD:architecture-start ... -->` and `<!-- GSD:architecture-end -->` markers still present
    - `CLAUDE.md` `## Architecture` section contains the literal substring `ARCHITECTURE.md` (pointer to the new file)
    - `CLAUDE.md` `## Conventions` section contains at least one of: `TypeScript strict`, `kebab-case`, `goja-safe`, `lib/simulations`
    - All other `<!-- GSD:*-start/end -->` markers (Project, Stack, Workflow, Profile) are present and unchanged
  </acceptance_criteria>
  <done>package.json `description` field reads as recruiter-tuned (Core Value alignment); CLAUDE.md Conventions and Architecture placeholder text is replaced with established-pattern content + ARCHITECTURE.md pointer respectively; all GSD comment markers preserved; package.json still parses as valid JSON.</done>
</task>

<task type="auto">
  <name>Task 5: Final cross-reference integrity sweep + ROADMAP/STATE refresh + Phase 5 SUMMARY</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/05-showcase-docs-recruiter-polish/05-01-SUMMARY.md</files>
  <read_first>
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\ROADMAP.md (Phase 5 entry at lines 88-100 — checkbox + plan list to flip to `[x]`)
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\STATE.md (frontmatter `status`, `stopped_at`, `progress.completed_phases`, `progress.completed_plans` to advance; Current Position narrative to update)
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\REQUIREMENTS.md (DOCS-01 + DOCS-02 rows at lines 35-36 + traceability table lines 77-78 — flip to `[x]` with phase reference)
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\phases\05-showcase-docs-recruiter-polish\05-CONTEXT.md (D-09 carry-forward language — STATE.md narrative should mention that Phase 5 surfaced BUILD-02 / SCEN-02 / F-01 / F-02 in docs without retroactive closure)
    - C:\Users\pzhly\Documents\GitHub\easyk6\.planning\phases\03-smoke-scenarios-supported-execution\03-02-SUMMARY.md (SUMMARY structure template — Carry-forward block, Phase closure handoff section pattern)
    - C:\Users\pzhly\Documents\GitHub\easyk6\ARCHITECTURE.md (produced Task 1 — sanity check before SUMMARY says "ARCHITECTURE.md authored")
    - C:\Users\pzhly\Documents\GitHub\easyk6\README.md (produced Task 2 — sanity check)
    - C:\Users\pzhly\Documents\GitHub\easyk6\CLAUDE.md (produced Task 4 — sanity check)
  </read_first>
  <action>
This task runs LAST. It does three things: (a) one final cross-reference integrity sweep across the now-finalized docs, (b) flip ROADMAP/REQUIREMENTS checkboxes + advance STATE.md, (c) write `05-01-SUMMARY.md` to close the plan.

**Step (a) — Final cross-reference integrity sweep:**

Run a comprehensive grep across the four top-level repo-root docs (`README.md`, `ARCHITECTURE.md`, `CLAUDE.md`, `package.json`) and verify ALL of the following:

```powershell
$files = @('README.md','ARCHITECTURE.md','CLAUDE.md','package.json')
$ok = $true

# 1. No references to the three deleted files
foreach ($f in $files) {
  if (-not (Test-Path $f)) { continue }
  $body = Get-Content $f -Raw
  foreach ($t in @('QUICKSTART.md','CONTRIBUTING.md','PROJECT_STRUCTURE.md')) {
    if ($body -match [regex]::Escape($t)) {
      Write-Host "FAIL: $f references deleted $t"; $ok = $false
    }
  }
}

# 2. The three deleted files are actually gone
foreach ($t in @('QUICKSTART.md','CONTRIBUTING.md','PROJECT_STRUCTURE.md')) {
  if (Test-Path $t) { Write-Host "FAIL: $t still exists"; $ok = $false }
}

# 3. The two canonical docs exist
foreach ($t in @('README.md','ARCHITECTURE.md')) {
  if (-not (Test-Path $t)) { Write-Host "FAIL: canonical doc $t missing"; $ok = $false }
}

# 4. README links to ARCHITECTURE.md (markdown link form)
$readme = Get-Content README.md -Raw
if ($readme -notmatch '\[ARCHITECTURE\.md\]\(ARCHITECTURE\.md\)') {
  Write-Host 'FAIL: README does not link to ARCHITECTURE.md'; $ok = $false
}

# 5. Verbatim Phase 4 Quickstart preservation — three signature lines must match byte-for-byte
foreach ($vline in @(
  '| `npm run smoke` | **Supported** | Default smoke profile against the demo target (recruiter demo path). |',
  '| `npm run example:load` | _Example_ | Illustrative load profile (ramping-vus, 5 VUs, ~2 min). |',
  '| `npm run example:capacity` | _Example_ | Illustrative capacity profile (ramping-arrival-rate, find-the-ceiling, ~3 min). |'
)) {
  if ($readme -notmatch [regex]::Escape($vline)) {
    Write-Host "FAIL: verbatim Quickstart line missing or modified: $vline"; $ok = $false
  }
}

# 6. No stale phase-N framing
foreach ($stale in @('Phase 1 establishes','Phase 2 next plan','lands in the next Phase 2 plan')) {
  foreach ($f in $files) {
    if (-not (Test-Path $f)) { continue }
    $body = Get-Content $f -Raw
    if ($body -match [regex]::Escape($stale)) {
      Write-Host "FAIL: $f contains stale framing: $stale"; $ok = $false
    }
  }
}

# 7. ARCHITECTURE.md has its 5 sections in order
$arch = Get-Content ARCHITECTURE.md -Raw
$expected = @('## Adapted from `ir-perf-k6`','## Simplified on purpose','## Upstream reuse pipeline','## k6 1.5 runtime caveats','## Decision log')
$positions = @()
foreach ($h in $expected) {
  $idx = $arch.IndexOf($h)
  if ($idx -lt 0) { Write-Host "FAIL: ARCHITECTURE.md missing section $h"; $ok = $false }
  $positions += $idx
}
for ($i = 1; $i -lt $positions.Count; $i++) {
  if ($positions[$i] -lt $positions[$i-1]) { Write-Host "FAIL: ARCHITECTURE.md sections out of order at index $i"; $ok = $false }
}

if (-not $ok) { throw 'Final cross-reference integrity sweep failed' }
```

**Step (b) — Flip ROADMAP + REQUIREMENTS + advance STATE:**

1. `.planning/ROADMAP.md`:
   - Flip Phase 5 main entry at line 21 from `- [ ] **Phase 5: Showcase Docs & Recruiter Polish**` to `- [x] **Phase 5: Showcase Docs & Recruiter Polish** - ... _(completed YYYY-MM-DD)_` using today's date.
   - Flip line 100 plan entry from `- [ ] 05-01-PLAN.md -- Rewrite showcase docs, architecture narrative, and final polish pass` to `- [x] 05-01-PLAN.md -- Rewrite showcase docs, architecture narrative, and final polish pass _(completed YYYY-MM-DD)_`.
   - Update the Progress table row at line 113 from `| 5. Showcase Docs & Recruiter Polish | 0/1 | Pending | - |` to `| 5. Showcase Docs & Recruiter Polish | 1/1 | Complete | YYYY-MM-DD |`.

2. `.planning/REQUIREMENTS.md`:
   - Flip line 35 from `- [ ] **DOCS-01**: ...` to `- [x] **DOCS-01**: ... _(completed Phase 05 plan 01, YYYY-MM-DD)_`.
   - Flip line 36 from `- [ ] **DOCS-02**: ...` to `- [x] **DOCS-02**: ... _(completed Phase 05 plan 01, YYYY-MM-DD)_`.
   - Update the traceability table rows for DOCS-01 + DOCS-02 from `Pending` to `Complete`.
   - Append a "Last updated" footer line referencing Phase 5 plan 01.

3. `.planning/STATE.md`:
   - Update frontmatter `status` to reflect "Phase 5 COMPLETE (1/1 plan) — milestone v1.0 closed" with today's date.
   - Update `stopped_at` with a one-paragraph narrative of what Phase 5 produced (README rewrite + ARCHITECTURE.md new + 3 stale-doc deletions + package.json description polish + CLAUDE.md placeholder refresh; D-09 carry-forward surfaced honestly).
   - Advance `progress.completed_phases` from 4 to 5.
   - Advance `progress.completed_plans` from 10 to 11.
   - Update `last_updated` to today's ISO 8601 timestamp.
   - In the body's Current Position section, append a Phase 5 closure paragraph noting the v1 milestone is closed and the four carry-forward items remain open by intentional descope (BUILD-02 / SCEN-02 / F-01 / F-02 documented in ARCHITECTURE.md §2 + README §Known carry-forward).
   - In the Performance Metrics velocity table, add a row for Phase 05 P01 (rough estimate or actual measurement; use `~X min` for duration and the actual files-modified count from `git status`).

**Step (c) — Write `05-01-SUMMARY.md`:**

Create `.planning/phases/05-showcase-docs-recruiter-polish/05-01-SUMMARY.md` following the structure of `.planning/phases/03-smoke-scenarios-supported-execution/03-02-SUMMARY.md`. Required sections:

- `# Phase 05-01: Rewrite showcase docs, architecture narrative, and final polish pass — SUMMARY` (H1)
- `## What was delivered` — bullet list of every D-01..D-10 deliverable closed (rewrote README; authored ARCHITECTURE.md with 5 sections in D-07 order; deleted QUICKSTART.md / CONTRIBUTING.md / PROJECT_STRUCTURE.md; folded PROJECT_STRUCTURE.md content into ARCHITECTURE.md §3; refreshed package.json description; refreshed CLAUDE.md Conventions + Architecture placeholders; surfaced BUILD-02 / SCEN-02 / F-01 / F-02 in both README §Known carry-forward AND ARCHITECTURE.md §2 `### Known limitations & deferred work`).
- `## Success criteria mapping` — table mapping ROADMAP Phase 5 SC1-SC4 to delivered artifacts (SC1 README quickstart/upstream-reuse → README §Quickstart + §Upstream Reuse; SC2 architecture docs adapted/simplified → ARCHITECTURE.md §1 + §2; SC3 smoke-first + deferred Grafana → README §Quickstart + ARCHITECTURE.md §2 first bullet; SC4 polished not experimental → 3 stale docs deleted + Phase-N stale framing scrubbed + package.json/CLAUDE.md polish).
- `## Verbatim preservation evidence` — quote the three signature lines from README §Quickstart and confirm byte-identical match against pre-Phase-5 README:13-15.
- `## Files modified` — 7-file list: ARCHITECTURE.md (NEW), README.md (REWRITE), QUICKSTART.md (DELETED), CONTRIBUTING.md (DELETED), PROJECT_STRUCTURE.md (DELETED), package.json (description polish), CLAUDE.md (Conventions + Architecture placeholder refresh).
- `## Carry-forward` — restate BUILD-02 / SCEN-02 / F-01 / F-02 with closure paths (using the same bullet shape as the README §Known carry-forward and ARCHITECTURE.md §2 subsection).
- `## v1 milestone closure` — one paragraph stating that Phase 5 closes the v1 milestone per ROADMAP, all 4 success criteria are observably TRUE, and v2 work (OBS-01/02, FRAME-01/02/03) is documented as out-of-scope in ARCHITECTURE.md §2.

Replace all `YYYY-MM-DD` placeholders with today's actual date as captured by the executor at run time (Get-Date -Format 'yyyy-MM-dd').
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ok=$true; $r=Get-Content .planning/ROADMAP.md -Raw; if ($r -notmatch '- \[x\] \*\*Phase 5: Showcase Docs') { Write-Host 'FAIL: ROADMAP Phase 5 not checked'; $ok=$false }; if ($r -notmatch '- \[x\] 05-01-PLAN\.md') { Write-Host 'FAIL: ROADMAP plan not checked'; $ok=$false }; if ($r -match '\| 5\. Showcase Docs & Recruiter Polish \| 0/1 \| Pending') { Write-Host 'FAIL: ROADMAP progress table not advanced'; $ok=$false }; $req=Get-Content .planning/REQUIREMENTS.md -Raw; if ($req -notmatch '- \[x\] \*\*DOCS-01\*\*') { Write-Host 'FAIL: DOCS-01 not checked'; $ok=$false }; if ($req -notmatch '- \[x\] \*\*DOCS-02\*\*') { Write-Host 'FAIL: DOCS-02 not checked'; $ok=$false }; $s=Get-Content .planning/STATE.md -Raw; if ($s -notmatch 'completed_phases: 5') { Write-Host 'FAIL: STATE completed_phases not advanced'; $ok=$false }; if ($s -notmatch 'completed_plans: 11') { Write-Host 'FAIL: STATE completed_plans not advanced'; $ok=$false }; if (-not (Test-Path '.planning/phases/05-showcase-docs-recruiter-polish/05-01-SUMMARY.md')) { Write-Host 'FAIL: 05-01-SUMMARY.md not created'; $ok=$false } else { $sum=Get-Content '.planning/phases/05-showcase-docs-recruiter-polish/05-01-SUMMARY.md' -Raw; foreach ($p in @('## What was delivered','## Success criteria mapping','## Verbatim preservation evidence','## Files modified','## Carry-forward','## v1 milestone closure','BUILD-02','SCEN-02','F-01','F-02')) { if ($sum -notmatch [regex]::Escape($p)) { Write-Host \"FAIL: SUMMARY missing $p\"; $ok=$false } } }; if ($ok) { Write-Host 'PASS' } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 5 main entry shows `- [x] **Phase 5: Showcase Docs & Recruiter Polish**` with `_(completed YYYY-MM-DD)_` suffix
    - `.planning/ROADMAP.md` plan list shows `- [x] 05-01-PLAN.md -- ... _(completed YYYY-MM-DD)_`
    - `.planning/ROADMAP.md` progress table row 5 shows `1/1 | Complete | YYYY-MM-DD`
    - `.planning/REQUIREMENTS.md` DOCS-01 row shows `- [x] **DOCS-01**` with Phase 05 plan 01 reference
    - `.planning/REQUIREMENTS.md` DOCS-02 row shows `- [x] **DOCS-02**` with Phase 05 plan 01 reference
    - `.planning/REQUIREMENTS.md` traceability table rows for DOCS-01 + DOCS-02 show `Complete`
    - `.planning/STATE.md` frontmatter `progress.completed_phases: 5`
    - `.planning/STATE.md` frontmatter `progress.completed_plans: 11`
    - `.planning/STATE.md` body Current Position has a Phase 5 closure paragraph
    - `.planning/phases/05-showcase-docs-recruiter-polish/05-01-SUMMARY.md` exists with all required sections (`## What was delivered`, `## Success criteria mapping`, `## Verbatim preservation evidence`, `## Files modified`, `## Carry-forward`, `## v1 milestone closure`)
    - SUMMARY mentions all four carry-forward IDs (BUILD-02, SCEN-02, F-01, F-02)
    - The final cross-reference sweep PowerShell script completes with `PASS` (no FAIL lines printed)
  </acceptance_criteria>
  <done>Final integrity sweep passes; ROADMAP + REQUIREMENTS + STATE reflect Phase 5 closure; 05-01-SUMMARY.md captures deliverables + success-criteria mapping + verbatim-preservation evidence + carry-forward + v1 milestone closure narrative; v1 milestone is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → recruiter eyes (GitHub render) | Markdown rendered on github.com — any embedded URLs, license claims, or copyright assertions in README/ARCHITECTURE.md are surfaced to recruiters |
| Repo → downstream readers (git clone) | Cross-references between docs must resolve after deletions (Tasks 1–4 land sequenced edits; Task 3 + Task 5 sweep dangling links) |
| Repo → planning history | `.planning/` is intentionally history-preserving; deleted files MAY remain referenced there without remediation |

## STRIDE Threat Register

Phase 5 is docs-only — no executable code, no auth surface, no data flow. The threat register is proportional: cross-reference integrity, embedded-URL hygiene, license/attribution accuracy.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering (I — Information integrity) | README.md §Quickstart verbatim block | mitigate | Task 2 acceptance criteria asserts byte-identical preservation of three signature table rows via regex match; Task 5 final sweep re-asserts the same three lines against the post-rewrite README. Any deviation FAILS Task 2 verify. |
| T-05-02 | Information disclosure | Embedded URLs in README + ARCHITECTURE.md | accept | URLs are: `https://othrondir.github.io/QAbbalah/` (public demo target, locked since Phase 1, no auth surface) + `https://github.com/Othrondir/easyPlaywright.git` (public upstream repo) + Grafana docs URLs from CLAUDE.md (public canonical references). No internal hostnames, no credentials, no API keys, no PII. Reviewed CLAUDE.md Tech Stack URLs: all public docs. |
| T-05-03 | Repudiation / dangling-reference rot | Cross-references to deleted files (QUICKSTART.md, CONTRIBUTING.md, PROJECT_STRUCTURE.md) | mitigate | Task 3 runs a pre-deletion gate (Tasks 1/2 must scrub references first) AND a post-deletion sweep across README, ARCHITECTURE, CLAUDE, package.json. Task 5 runs a final independent sweep with a hard-fail PowerShell script. Three independent gates is sufficient. |
| T-05-04 | Tampering (license / attribution accuracy) | License field in package.json, copyright claims in README | accept | `package.json` line 26 already shows `"license": "MIT"`; Phase 5 does not touch this field. No copyright assertions added in README or ARCHITECTURE.md beyond the existing project framing. Upstream `easyPlaywright` is referenced as the permanent POM source (factual, not a license claim). No `LICENSE` file is added or modified. |
| T-05-05 | Information disclosure (leaked internal context in placeholder refresh) | CLAUDE.md Conventions + Architecture sections | mitigate | Task 4 Conventions content is sourced entirely from already-public Phase 1-4 patterns (TypeScript strict, kebab-case scenario IDs, goja-safe, profile-keyed entries, patch-injection). Architecture content is a one-sentence pointer to ARCHITECTURE.md. No employer names, no internal URLs, no team conventions. CLAUDE.md GSD comment markers (`<!-- GSD:*-start/end -->`) are preserved exactly so downstream GSD tooling continues to parse blocks correctly. |
| T-05-06 | Denial of service (recruiter readability) | README + ARCHITECTURE.md length / scannability | accept | Recruiter reads top-to-bottom in ~30 seconds per CONTEXT §Specifics. README sections are scoped tightly (Quickstart, Upstream Reuse, Commands, Runtime Config, Architecture, Legacy Note, Known carry-forward — no wall-of-text sections). ARCHITECTURE.md is 5 H2 sections with bullet-driven content. No threat to readability beyond planner discretion. |
| T-05-07 | Elevation of privilege (none — no auth surface) | n/a | accept | Phase 5 introduces zero authentication, authorization, or privilege boundaries. Docs-only phase. |

**Disposition summary:** 3 mitigate (T-05-01, T-05-03, T-05-05) — all with concrete in-task acceptance criteria. 4 accept — all with reviewed rationale (no PII, no credentials, no internal context, no auth surface).
</threat_model>

<verification>
## Phase 5 verification map (must_haves → ROADMAP Success Criteria)

| Success Criterion (ROADMAP Phase 5) | must_have(s) | Verified by |
|--------------------------------------|--------------|-------------|
| SC1: README/quickstart explains upstream reuse + smoke demo path clearly | truths #1, #3; artifact `README.md` | Task 2 verify (verbatim Quickstart preservation) + Task 2 acceptance (§Quickstart + §Upstream Reuse + smoke as Supported) |
| SC2: Architecture docs explain adapted-from-ir-perf-k6 + simplified-on-purpose | truths #2; artifact `ARCHITECTURE.md` | Task 1 verify (5 sections in D-07 order) + Task 1 acceptance (§1 lifted patterns + §2 six rejection bullets) |
| SC3: Smoke-first scope + deferred Grafana visible without ambiguity | truths #6, #10 | Task 1 §2 first bullet (`No Grafana / OTEL`) + Task 2 §Quickstart table (`**Supported**` vs `_Example_` distinction) + Task 5 sweep |
| SC4: Final repo feels polished rather than experimental | truths #4, #5, #7 (stale docs deleted; Phase-1 framing scrubbed; package.json + CLAUDE.md polished) | Task 3 acceptance (three deletions) + Task 4 acceptance (description + placeholders refreshed) + Task 5 final sweep (no stale framing remains) |

**Cross-cutting verification gates:**
- Verbatim preservation: Task 2 verify + Task 5 step (a) item 5 (three signature lines must match byte-for-byte)
- Dangling-reference cleanup: Task 3 pre-deletion gate + Task 3 post-deletion sweep + Task 5 step (a) item 1 (three independent gates)
- ROADMAP / REQUIREMENTS / STATE consistency: Task 5 step (b) — flips DOCS-01/DOCS-02 to `[x]`, advances completed_phases to 5 and completed_plans to 11
</verification>

<success_criteria>
Phase 5 plan 01 is complete when:

1. `ARCHITECTURE.md` exists at repo root with exactly 5 H2 sections in D-07 order; §3 contains the four boundary labels (src/pages, lib/pages, lib/pages-k6-patches, legacy-js) folded from PROJECT_STRUCTURE.md; §4 cites Phase 03-02 + Phase 04 evidence without re-proving; §5 indexes `.planning/` as a pointer list with at least 6 entries.
2. `README.md` is rewritten top-to-bottom with the Phase 4 §Quickstart table preserved byte-identical (verified by three regex matches against the exact signature lines); §Architecture links to ARCHITECTURE.md; §Known carry-forward surfaces BUILD-02 / SCEN-02 / F-01 / F-02.
3. `QUICKSTART.md`, `CONTRIBUTING.md`, `PROJECT_STRUCTURE.md` are deleted from the working tree; zero dangling references remain in README, ARCHITECTURE.md, CLAUDE.md, or package.json.
4. `package.json` `description` is refreshed to recruiter-tuned wording; the file still parses as valid JSON; all other manifest fields are byte-identical to pre-Phase-5.
5. `CLAUDE.md` Conventions and Architecture placeholder text is replaced with established-pattern content + ARCHITECTURE.md pointer; all `<!-- GSD:*-start/end -->` markers are preserved exactly.
6. `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and `.planning/STATE.md` reflect Phase 5 closure (checkboxes flipped, progress advanced from 4/5 phases + 10 plans to 5/5 phases + 11 plans, traceability rows for DOCS-01 + DOCS-02 marked Complete).
7. `.planning/phases/05-showcase-docs-recruiter-polish/05-01-SUMMARY.md` exists with the six required sections (What was delivered / Success criteria mapping / Verbatim preservation evidence / Files modified / Carry-forward / v1 milestone closure).
8. Final cross-reference integrity sweep (Task 5 step (a)) passes with no FAIL lines.

The v1 milestone is then closed. All four ROADMAP Phase 5 success criteria are observably TRUE.
</success_criteria>

<output>
After completion, create `.planning/phases/05-showcase-docs-recruiter-polish/05-01-SUMMARY.md` (Task 5 step (c)). The SUMMARY captures deliverables, ROADMAP success-criteria mapping, verbatim-preservation evidence, files modified, carry-forward, and v1 milestone closure.
</output>
